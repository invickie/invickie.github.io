<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>UNP网络编程练习(回显程序)</title>
      <link href="2021/06/04/unp2/"/>
      <url>2021/06/04/unp2/</url>
      
        <content type="html"><![CDATA[<h1 id="UNP网络编程练习-回显程序"><a href="#UNP网络编程练习-回显程序" class="headerlink" title="UNP网络编程练习(回显程序)"></a>UNP网络编程练习(回显程序)</h1><p>通过实现书中的demo来加深对网络编程的理解</p><h2 id="实现环境"><a href="#实现环境" class="headerlink" title="实现环境"></a>实现环境</h2><p>本地虚拟机<br>阿里云服务器</p><h2 id="基础知识"><a href="#基础知识" class="headerlink" title="基础知识"></a>基础知识</h2><p>TCP三次握手</p><ul><li>服务器必须准备好接受外来的连接。通常通过调用socket,bind和listen这三个函数来完成，我们称之为被动打开。</li><li>客户通过调用connect发起主动打开。这导致客户TCP发送一个SYN（同步）分节，它告诉服务器客户将在（待建立的）连接中发送的数据的初始序列号。通常SYN分节不携带数据，其所在IP数据报只含有一个IP首部，一个TCP首部及可能有的TCP选项。<ul><li>TCP选项之MSS选项：发送SYN的TCP一端使用本选项通告对端它的最大分节大小，也就是它在本连接的每个TCP分节中愿意接受的最大数据量。发送端TCP使用接收端的MSS值作为所发送分节的最大大小。</li></ul></li><li>服务器必须确认（ACK）客户的SYN，同时自己也得发送一个SYN分节，它含有服务器将在同一连接中发送的数据的初始序列号。服务器在单个分节中发送SYN和对客户SYN的ACK</li><li>客户必须确认服务器的SYN。</li></ul><p><img src="https://tuchuang-1300238575.cos.ap-beijing.myqcloud.com/myblog/tcp%E4%B8%89%E6%AC%A1%E6%8F%A1%E6%89%8B.jpg"></p><p>TCP四次挥手</p><ul><li>某个应用进程首先调用close，我们称该端执行主动关闭。该端的TCP于是发送一个FIN分节，表示数据发送完毕。</li><li>接收到这个FIN的对端执行被动关闭。这个FIN由TCP确认。它的接收也作为一个文件结束符（EOF）传递给接收端应用进程（放在已排队等候该应用进程接收的任何其他数据之后），因为FIN的接收意味着接收端应用进程在相应连接上再无额外数据可接收。</li><li>一段时间后，接收到这个文件结束符的应用进程将调用close关闭它的套接字。这导致它的TCP也发送一个FIN。</li><li>接收这个最终FIN的原发送端TCP（即执行主动关闭的那一端）确认这个FIN。<br>note：从执行被动关闭到执行主动关闭（步骤2和步骤3之间）一端流动数据是可能的，这称为半关闭，毕竟当时接收端的套接字并未close掉。</li></ul><p><img src="https://tuchuang-1300238575.cos.ap-beijing.myqcloud.com/myblog/TCP%E5%9B%9B%E6%AC%A1%E6%8C%A5%E6%89%8B.jpg"></p><p>TCP状态转换图</p><p><img src="https://tuchuang-1300238575.cos.ap-beijing.myqcloud.com/myblog/tcp%E7%8A%B6%E6%80%81%E5%9B%BE.png"></p><p><img src="https://tuchuang-1300238575.cos.ap-beijing.myqcloud.com/myblog/tcp%E8%BF%9E%E6%8E%A5%E5%88%86%E7%BB%84%E4%BA%A4%E6%8D%A2.jpg"></p><p>socket函数</p><ul><li>为了执行网络I/O，一个进程必须做的第一件事就是调用socket函数，指定期望的通信协议类型。<pre class=" language-c++"><code class="language-c++">#include <sys/socket.h>int socket( int family, int type, int protocol );返回：若成功则为非负描述符，若出错则为-1</code></pre></li></ul><p>connect函数</p><ul><li>客户端用connect函数来建立与TCP服务器的连接</li><li>sockfd是由socket函数返回的套接字描述符，第二个，第三个参数分别是一个指向套接字地址结构的指针和该结构的大小。</li><li>客户在调用函数connect前不必非得调用bind函数，因为如果需要的话，内核会确定源IP地址，并选择一个临时端口作为源端口。</li><li>如果是TCP套接字，调用connect函数将激发TCP的三路握手过程，而且仅在连接建立成功或出错时才返回<pre class=" language-c++"><code class="language-c++">#include <sys/socket.h>int connect( int sockfd, const struct sockaddr *servaddr, socklen_t addrlen );返回：若成功则为0，若出错则为-1</code></pre></li></ul><p>bind函数</p><ul><li>bind函数把一个本地协议地址赋予一个套接字。</li><li>第二个参数是一个指向特定与协议的地址结构的指针，第三个参数是该地址结构的长度。</li><li>服务器在启动时捆绑它的众所周知端口（端口用于标识一个进程，如果端口为0，则由内核选择端口,而且必须使用getsockname来返回协议地址来得到内核所选择的这个端口号）</li><li>进程可以把一个特定的IP地址捆绑到它的套接字上（一般都是通配地址，用常量值INADDR_ANY来指定，如htonl( INADDR_ANY)）<br>```c++<br>#include &lt;sys/socket.h&gt;</li></ul><p>int bind( int sockfd, const struct sockaddr *myaddr, socklen_t addrlen );<br>返回：若成功则为0，若出错则为-1</p><pre><code>listen函数- listen函数仅由TCP服务器调用  - 当socket函数创建一个套接字时，它被假设为一个主动套接字，也就是说，它是一个将调用connect发起连接的客户套接字。listen函数把一个未连接的套接字转换成一个被动套接字，指示内核应接受指向该套接字的连接请求。  - 第二个参数规定了内核应该为相应套接字排队的最大连接个数。```c++#include &lt;sys/socket.h&gt;int listen( int sockfd, int backlog ); 返回：若成功则为0，若出错则为-1</code></pre><p>note:理解backlog<br>内核为任何一个给定的监听套接字维护两个队列：</p><ul><li>未完成连接队列：每个这个的SYN分节对应其中一项：已由某个客户发出并到达服务器，而服务器正在等待完成相应的TCP三路握手过程。这些套接字处于SYN_RCVD状态。</li><li>已完成连接队列：每个已完成TCP三路握手过程的客户对应其中一项。这些套接字处于ESTABLISHED状态。</li></ul><p><img src="https://tuchuang-1300238575.cos.ap-beijing.myqcloud.com/myblog/%E4%B8%A4%E4%B8%AA%E9%98%9F%E5%88%97.png"></p><p>accept函数</p><ul><li>accept函数由TCP服务器调用，用于从已完成连接队列队头返回下一个已完成连接。如果已完成连接队列为空，那么进程被投入睡眠（假定套接字为默认的阻塞方式）</li><li>参数cliaddr和addrlen用来返回已连接的对端进程（客户）的协议地址。addrlen是值-结果参数：调用前，我们将由*addrlen所引用的整数值置为由cliaddr所指的套接字地址结构的长度，返回时，该整数值即为由内核存放在该套接字地址结构内的确切字节数<pre class=" language-c++"><code class="language-c++">#include <sys/socket.h>int accept( int sockfd, struct sockaddr *cliaddr, socklen_t *addrlen );返回：若成功则为非负描述符，若出错则为-1</code></pre></li></ul><p>close函数</p><ul><li>close函数也用来关闭套接字，并终止TCP连接<pre class=" language-c++"><code class="language-c++">#include <unistd.h>int close( int sockfd );</code></pre></li></ul><p>getsockname和getpeername函数</p><ul><li>这两个函数或者返回与某个套接字关联的本地协议地址(getsockname)，或者返回与某个套接字关联的外地协议地址（getpeername）</li><li>测试函数如下<br>server<br>```c++<br>#include &lt;stdio.h&gt;<br>#include &lt;stdlib.h&gt;<br>#include &lt;time.h&gt;<br>#include &lt;sys/socket.h&gt;<br>#include &lt;sys/types.h&gt;<br>#include &lt;fcntl.h&gt;<br>#include &lt;netinet/in.h&gt;</li></ul><p>#define MAXLINE 1024<br>#define SA struct sockaddr</p><p>int main(int argc, char **argv)<br>{<br>    int        listenfd, connfd;<br>    int        buff[MAXLINE];<br>    pid_t    pid;<br>    time_t    ticks;<br>    struct    sockaddr_in    servaddr;<br>    struct    sockaddr_in    cliaddr;<br>    socklen_t    cliLen;<br>    listenfd = socket(AF_INET, SOCK_STREAM, 0);</p><pre><code>bzero(&amp;servaddr, sizeof(servaddr));servaddr.sin_family = AF_INET;servaddr.sin_addr.s_addr = htonl(INADDR_ANY);servaddr.sin_port = htons(9010);bind(listenfd, (SA *)&amp;servaddr, sizeof(servaddr));listen(listenfd, 5);for ( ; ; )&#123;    cliLen = sizeof(cliaddr);    connfd = accept(listenfd, (SA *)&amp;cliaddr, &amp;cliLen);    if ((pid = fork()) == 0)&#123;        close(listenfd);        ticks = time(NULL);        snprintf(buff, sizeof(buff), &quot;%.24s\r\n&quot;, ctime(&amp;ticks));        write(connfd, buff, strlen(buff));        _exit(0);    &#125;    if (waitpid(pid, NULL, 0) != pid)&#123;        printf(&quot;waitpid error\n&quot;);        exit(1);    &#125;    close(connfd);&#125;return 0;</code></pre><p>}</p><pre><code>client```c++#include &lt;stdio.h&gt;#include &lt;stdlib.h&gt;#include &lt;netinet/in.h&gt;#include &lt;fcntl.h&gt;#define MAXLINE 1024#define SA struct sockaddrint main(int argc, char **argv)&#123;    int        sockfd, n;    struct    sockaddr_in    servaddr;    char    buff[MAXLINE + 1];    struct    sockaddr_in    cliaddr;    socklen_t    cliLen;    sockfd = socket(AF_INET, SOCK_STREAM, 0);    bzero(&amp;servaddr, sizeof(servaddr));    servaddr.sin_family = AF_INET;    servaddr.sin_port = htons(9010);    inet_pton(AF_INET, argv[1], &amp;servaddr.sin_addr);    connect(sockfd, (SA *)&amp;servaddr, sizeof(servaddr));    cliLen = sizeof(cliaddr);    getsockname(sockfd, (SA *)&amp;cliaddr, &amp;cliLen);    while ((n = read(sockfd, buff, MAXLINE)) &gt; 0)&#123;        buff[n] = &#39;\0&#39;;        fputs(buff, stdout);    &#125;    return 0;&#125;</code></pre><p>当我们分别用gdb调试的时候，发现服务端和客户端的cliaddr的内容是一致的:</p><pre><code>(gdb) p cliaddr$2 = &#123;sin_family = 2, sin_port = 42735, sin_addr = &#123;s_addr = 16748343&#125;,sin_zero = &quot;\000\000\000\000\000\000\000&quot;&#125;</code></pre><p>处理信号</p><ul><li><p>POSIX信号处理<br>信号就是告知某个进程发生了某个事件的通知，有时也称为软件中断。信号通常是异步发生的，也就是说进程预先不知道信号的准确发生时刻</p></li><li><p>处理SIGCHLD信号<br>设置僵尸状态的目的是维护子进程的信息，以便父进程在以后某个时候获取。如果一个进程终止，而该进程有子进程处于僵尸状态，那么它的所有僵尸子进程的父进程ID将被重置为1（init进程），而僵尸进程出现时间是在子进程终止后，但是父进程尚未读取这些数据之前。处理办法是保证父进程处理这些数据，我们可以通过wait或者waitpid函数来达到这个要求。</p></li></ul><h2 id="回显程序"><a href="#回显程序" class="headerlink" title="回显程序"></a>回显程序</h2><p>server</p><pre class=" language-c++"><code class="language-c++">#include <stdio.h>#include <netinet/in.h>#include <stdlib.h>#include <sys/socket.h>#include <signal.h>#include <errno.h>#define MAXLINE 1024#define SA struct sockaddrvoid sig_chld(int signo);typedef void Sigfunc(int);Sigfunc *Signal(int signo, Sigfunc *func);void str_echo(int sockfd);int main(int argc, char **argv)&#123;    int        listenfd, connfd;    pid_t    childpid;    socklen_t    clilen;    struct    sockaddr_in    servaddr, cliaddr;    listenfd = socket(AF_INET, SOCK_STREAM, 0);    bzero(&servaddr, sizeof(servaddr));    servaddr.sin_family = AF_INET;    servaddr.sin_port = htons(9010);    servaddr.sin_addr.s_addr = htonl(INADDR_ANY);    bind(listenfd, (SA *)&servaddr, sizeof(servaddr));    listen(listenfd, 5);    Signal(SIGCHLD, sig_chld);    for ( ; ; )&#123;        clilen = sizeof(cliaddr);        if ((connfd = accept(listenfd, (SA *)&cliaddr, &clilen)) < 0)&#123;            if (errno == EINTR)                continue;            else&#123;                printf("accept error\n");                exit(-1);            &#125;        &#125;        if ((childpid = fork()) == 0)&#123;            close(listenfd);            str_echo(connfd);            exit(0);        &#125;        close(connfd);    &#125;    return 0;&#125;void sig_chld(int signo)&#123;    pid_t    pid;    int        stat;    while ((pid = waitpid(-1, &stat, WNOHANG)) > 0)        printf("child %d terminated\n", pid);    return;&#125;Sigfunc *Signal(int signo, Sigfunc *func)&#123;    struct    sigaction act, oact;    act.sa_handler = func;    sigemptyset(&act.sa_mask);    act.sa_flags = 0;    if (signo == SIGALRM)&#123;#ifdef SA_INTERRUPT        act.sa_flags |= SA_INTERRUPT;#endif    &#125; else &#123;        #ifdef SA_RESTART        act.sa_flags |= SA_RESTART;#endif    &#125;    if (sigaction(signo, &act, &oact) < 0)        return (SIG_ERR);    return (oact.sa_handler);&#125;void str_echo(int sockfd)&#123;    char        buff[MAXLINE];    int            n;    for ( ; ; )&#123;        if ((n = read(sockfd, buff, MAXLINE)) > 0)&#123;            buff[n] = '\0';            write(sockfd, buff, n);        &#125;        else if (n < 0 && errno == EINTR)            continue;        else if (n < 0)&#123;            printf("str_echo:read error\n");            return;        &#125;        else if (n == 0)&#123;            break;        &#125;    &#125;&#125;</code></pre><p>client</p><pre class=" language-c++"><code class="language-c++">#include <stdio.h>#include <stdlib.h>#include <netinet/in.h>#include <fcntl.h>#define MAXLINE 1024#define SA struct sockaddrvoid str_cli(FILE *fp, int sockfd);int main(int argc, char **argv)&#123;    int        sockfd[5], n;    struct    sockaddr_in    servaddr;    struct    sockaddr_in    cliaddr;    int        i;    for (i = 0; i < 5; i++)&#123;        sockfd[i] = socket(AF_INET, SOCK_STREAM, 0);        bzero(&servaddr, sizeof(servaddr));        servaddr.sin_family = AF_INET;        servaddr.sin_port = htons(9010);        inet_pton(AF_INET, argv[1], &servaddr.sin_addr);        connect(sockfd[i], (SA *)&servaddr, sizeof(servaddr));    &#125;    str_cli(stdin, sockfd[0]);    return 0;&#125;void str_cli(FILE *fp, int sockfd)&#123;    char        sendline[MAXLINE], recvline[MAXLINE];    while (fgets(sendline, MAXLINE, fp) != NULL)&#123;        write(sockfd, sendline, strlen(sendline));        if (read(sockfd, recvline, MAXLINE) == 0)&#123;            printf("str_cli:server terminated prematurely\n");            return;        &#125;        fputs(recvline, stdout);    &#125;&#125;</code></pre><h2 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h2><p>server</p><pre><code>root@iZ2ze8jni9rk9rkg76dslmZ:~/home/unp/chart5# ./srv</code></pre><p>client</p><pre><code>root@iZ2ze8jni9rk9rkg76dslmZ:~/home/unp/chart5# ./cli 127.0.0.1hello worldhello world</code></pre><p>参考资料：<br>UNIX网络编程卷1 第3版</p>]]></content>
      
      
      <categories>
          
          <category> UNP </category>
          
      </categories>
      
      
        <tags>
            
            <tag> UNP, 网络编程 - Markdown </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>UNP网络编程练习(时间获取程序)</title>
      <link href="2021/06/04/unp1/"/>
      <url>2021/06/04/unp1/</url>
      
        <content type="html"><![CDATA[<h1 id="UNP网络编程练习-时间获取程序"><a href="#UNP网络编程练习-时间获取程序" class="headerlink" title="UNP网络编程练习(时间获取程序)"></a>UNP网络编程练习(时间获取程序)</h1><p>通过实现书中的demo来加深对网络编程的理解</p><h2 id="实现环境"><a href="#实现环境" class="headerlink" title="实现环境"></a>实现环境</h2><p>本地虚拟机<br>阿里云服务器</p><h2 id="时间获取程序"><a href="#时间获取程序" class="headerlink" title="时间获取程序"></a>时间获取程序</h2><p>server</p><pre class=" language-c++"><code class="language-c++">#include <time.h>#include <stdio.h>#include <sys/types.h>#include <sys/socket.h>#include <netinet/in.h>#include <stdlib.h>#include <fcntl.h>#define SA struct sockaddr#define MAXLINE 1024int main() &#123;    int listenfd, connfd;    struct sockaddr_in servaddr;    char buff[MAXLINE];    time_t ticks;    pid_t pid;    listenfd = socket(AF_INET, SOCK_STREAM, 0);    bzero(&servaddr, sizeof(servaddr));    servaddr.sin_family = AF_INET;    servaddr.sin_addr.s_addr = htonl(INADDR_ANY);    servaddr.sin_port = htons(9010);    bind (listenfd, (SA*)& servaddr, sizeof(servaddr));    listen(listenfd, 5);    for( ; ; ) &#123;        connfd = accept(listenfd, (SA*)NULL, NULL);        pid = fork();        if (0 == pid) &#123;            ticks = time(NULL);            snprintf(buff, sizeof(buff), "%24s\r\n", ctime(&ticks));            write(connfd, buff, strlen(buff));            exit(1);        &#125;        if (waitpid(pid, NULL, 0) != pid) &#123;            printf("waitpid error");            exit(1);        &#125;        close(connfd);    &#125;    return 0;&#125;   </code></pre><p>client</p><pre class=" language-c++"><code class="language-c++">#include <stdio.h>#include <sys/types.h>#include <sys/socket.h>#include <netinet/in.h>#include <stdlib.h>#define SA struct sockaddr#define MAXLINE 1024int main(int argc, char **argv) &#123;    int sockfd, n;    char recvline[MAXLINE + 1];    struct sockaddr_in servaddr;        if (argc != 2) &#123;        printf("usage:a.out <IPaddress>");        exit(1);    &#125;    if ((sockfd = socket(AF_INET, SOCK_STREAM, 0)) < 0) &#123;        printf("socket error\n");        exit(1);    &#125;    bzero(&servaddr, sizeof(servaddr));    servaddr.sin_family = AF_INET;    servaddr.sin_port = htons(9010);    if (inet_pton(AF_INET, argv[1], &servaddr.sin_addr) <= 0) &#123;        printf("inet_pton error for %s", argv[1]);        exit(1);    &#125;    if (connect(sockfd, (SA*)& servaddr, sizeof(servaddr)) < 0) &#123;        printf("connect error\n");        exit(1);    &#125;    while ((n = read(sockfd, recvline, MAXLINE)) > 0) &#123;        recvline[n] = 0;        if (fputs(recvline, stdout) == EOF) &#123;            printf("fputs error\n");            exit(1);        &#125;    &#125;    if (n < 0) &#123;        printf("read error\n");        exit(1);    &#125;    exit(0);&#125;</code></pre><h2 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h2><ul><li><p>字节排序函数（大小端转换）</p><ul><li>大端：高字节放低地址</li><li>小段：高字节放高地址<br>网络传输采用大端字节序<br>```c++<br>#include &lt;netinet/in.h&gt;</li></ul><p>uint16_t htons( uint16_t host16bitvalue );<br>uint32_t htonl( uint32_t host32bitvalue );</p><pre><code>                  ------------均返回：网络字节序的值</code></pre><p>uint16_t ntohs( uint16_t net16bitvalue );<br>uint32_t ntohl( uint32_t net32bitvalue );</p><pre><code>                   ------------均返回：主机字节序的值</code></pre><pre><code>测试```c++#include &lt;stdio.h&gt;#include &lt;netinet/in.h&gt;#include &lt;sys/socket.h&gt;int main(int argc, char **argv) &#123;  uint32_t    ipAddr;  uint16_t    portAddr;  struct sockaddr_in    servaddr;  char        buff[1024];  bzero(&amp;servaddr, sizeof(servaddr));  inet_pton(AF_INET, argv[1], &amp;servaddr.sin_addr);  ipAddr = ntohl(servaddr.sin_addr.s_addr);  printf(&quot;ipAddr is:%d\n&quot;, ipAddr);  bzero(&amp;servaddr, sizeof(servaddr));  servaddr.sin_addr.s_addr = htonl(ipAddr);  inet_ntop(AF_INET, &amp;servaddr.sin_addr, buff, sizeof(buff));  printf(&quot;the ip addr is:%s\n&quot;, buff);  printf(&quot;------------\n&quot;);  portAddr = htons(9010);  printf(&quot;the port 9010 netword port is:%d\n&quot;, portAddr);  printf(&quot;the port is:%d\n&quot;, ntohs(portAddr));  return 0;&#125;</code></pre><p>结果</p><pre><code>ygq@ygq-virtual-machine:~/unp/chat1$ ./test1 127.0.0.1 ipAddr is:2130706433 the ip addr is:127.0.0.1 ------------ the port 9010 netword port is:12835 the port is:9010</code></pre></li><li><p>调用sprintf无法检查目的缓冲区是否溢出，而snprintf要求其第二个参数指定目的缓冲区的大小，因此可以确保该缓冲区不溢出。</p></li></ul><h2 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h2><ul><li>本地<br>-server<pre><code>ygq@ygq-virtual-machine:~/unp/chat1$ gcc srv.c -o srvygq@ygq-virtual-machine:~/unp/chat1$ ./srv </code></pre>-client<pre><code>ygq@ygq-virtual-machine:~/unp/chat1$ gcc cli.c -o cliygq@ygq-virtual-machine:~/unp/chat1$ ./cli 127.0.0.1Fri Sep  4 10:14:14 2020</code></pre></li><li>云服务器(本地为client)<br>-server<pre><code>root@iZ2ze8jni9rk9rkg76dslmZ:~/home/unp/chart1# gcc srv.c -o srvroot@iZ2ze8jni9rk9rkg76dslmZ:~/home/unp/chart1# ./srv </code></pre>-client<pre><code>ygq@ygq-virtual-machine:~/unp/chat1$ gcc cli.c -o cliygq@ygq-virtual-machine:~/unp/chat1$ ./cli 182.258.165.464(假的，别搞我)Fri Sep  4 10:30:24 2020</code></pre></li></ul><p>注意测试时保证端口没被占用</p><p>参考资料：<br>UNIX网络编程卷1 第3版</p>]]></content>
      
      
      <categories>
          
          <category> UNP </category>
          
      </categories>
      
      
        <tags>
            
            <tag> UNP, 网络编程 - Markdown </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>STL容器源码剖析 (priority_queue容器配接器)</title>
      <link href="2021/06/01/priority-queue/"/>
      <url>2021/06/01/priority-queue/</url>
      
        <content type="html"><![CDATA[<h1 id="STL容器源码剖析-priority-queue容器配接器"><a href="#STL容器源码剖析-priority-queue容器配接器" class="headerlink" title="STL容器源码剖析 (priority_queue容器配接器)"></a>STL容器源码剖析 (priority_queue容器配接器)</h1><p>STL在日程编程应用非常的广泛,之前看到一篇大牛文章说，用C++开发，尽量用容器类+迭代器来代替数组+指针，因为数组+指针容易越界，或者内存泄露，相反，容器类和+迭代器都有大神将底层封装好，使用安全简单。</p><p>priority_queue是拥有优先级的queue，不过它容器内的元素并不是根据加入顺序排列，而是根据用户定义的优先级进行排列。只能在队列尾部加入元素，在头部取出元素。不能遍历容器。priority_queue是基于某种容器作为底部结构的，默认容器是vector容器，用户也可以自己指定容器的类型。参考文档<a href="http://www.cplusplus.com/reference/queue/priority_queue/">priority_queue</a></p><h2 id="priority-queue容器配接器"><a href="#priority-queue容器配接器" class="headerlink" title="priority_queue容器配接器"></a>priority_queue容器配接器</h2><p>由于只是在vector容器基础的接口实现，重点看元素的调整，源码如下</p><pre class=" language-c++"><code class="language-c++">template <class _Tp,           class _Sequence __STL_DEPENDENT_DEFAULT_TMPL(vector<_Tp>),          class _Compare          __STL_DEPENDENT_DEFAULT_TMPL(less<typename _Sequence::value_type>) >class priority_queue &#123;  // requirements:  __STL_CLASS_REQUIRES(_Tp, _Assignable);  __STL_CLASS_REQUIRES(_Sequence, _Sequence);  __STL_CLASS_REQUIRES(_Sequence, _RandomAccessContainer);  typedef typename _Sequence::value_type _Sequence_value_type;  __STL_CLASS_REQUIRES_SAME_TYPE(_Tp, _Sequence_value_type);  __STL_CLASS_BINARY_FUNCTION_CHECK(_Compare, bool, _Tp, _Tp);public:    // priority_queue仅支持对头部和尾部的操作, 所以不定义STL要求的    // pointer, iterator, difference_type   typedef typename _Sequence::value_type      value_type;  typedef typename _Sequence::size_type       size_type;  typedef          _Sequence                  container_type;  typedef typename _Sequence::reference       reference;  typedef typename _Sequence::const_reference const_reference;protected:  _Sequence c;//底层容器，默认为vector，用户可自行指定容器类型  _Compare comp;//优先级决策方式public:    //*******************************    //*    构造函数    //*******************************  priority_queue() : c() &#123;&#125;  explicit priority_queue(const _Compare& __x) :  c(), comp(__x) &#123;&#125;  priority_queue(const _Compare& __x, const _Sequence& __s)     : c(__s), comp(__x)     &#123; make_heap(c.begin(), c.end(), comp); &#125;#ifdef __STL_MEMBER_TEMPLATES  template <class _InputIterator>  priority_queue(_InputIterator __first, _InputIterator __last)     : c(__first, __last) &#123; make_heap(c.begin(), c.end(), comp); &#125;  template <class _InputIterator>  priority_queue(_InputIterator __first,                  _InputIterator __last, const _Compare& __x)    : c(__first, __last), comp(__x)     &#123; make_heap(c.begin(), c.end(), comp); &#125;  template <class _InputIterator>  priority_queue(_InputIterator __first, _InputIterator __last,                 const _Compare& __x, const _Sequence& __s)  : c(__s), comp(__x)  &#123;     c.insert(c.end(), __first, __last);    make_heap(c.begin(), c.end(), comp);  &#125;#else /* __STL_MEMBER_TEMPLATES */  priority_queue(const value_type* __first, const value_type* __last)     : c(__first, __last) &#123; make_heap(c.begin(), c.end(), comp); &#125;  priority_queue(const value_type* __first, const value_type* __last,                  const _Compare& __x)     : c(__first, __last), comp(__x)    &#123; make_heap(c.begin(), c.end(), comp); &#125;  priority_queue(const value_type* __first, const value_type* __last,                  const _Compare& __x, const _Sequence& __c)    : c(__c), comp(__x)   &#123;     c.insert(c.end(), __first, __last);    make_heap(c.begin(), c.end(), comp);  &#125;#endif /* __STL_MEMBER_TEMPLATES */  //判断容器是否为空  bool empty() const &#123; return c.empty(); &#125;  //返回容器元素的个数  size_type size() const &#123; return c.size(); &#125;  //返回优先级最高元素的引用  const_reference top() const &#123; return c.front(); &#125;  //新增元素，并根据优先级调整堆  void push(const value_type& __x) &#123;    __STL_TRY &#123;      c.push_back(__x);       push_heap(c.begin(), c.end(), comp);    &#125;    __STL_UNWIND(c.clear());  &#125;  //弹出优先级最高的元素  void pop() &#123;    __STL_TRY &#123;      pop_heap(c.begin(), c.end(), comp);      c.pop_back();    &#125;    __STL_UNWIND(c.clear());  &#125;&#125;;// no equality is provided__STL_END_NAMESPACE#endif /* __SGI_STL_INTERNAL_QUEUE_H */</code></pre><p>参考资料：<br>《STL源码剖析》侯捷<br>各位大佬博客</p>]]></content>
      
      
      <categories>
          
          <category> STL </category>
          
      </categories>
      
      
        <tags>
            
            <tag> STL, C++ - Markdown </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>STL容器源码剖析 (queue容器配接器)</title>
      <link href="2021/06/01/queue/"/>
      <url>2021/06/01/queue/</url>
      
        <content type="html"><![CDATA[<h1 id="STL容器源码剖析-queue容器配接器"><a href="#STL容器源码剖析-queue容器配接器" class="headerlink" title="STL容器源码剖析 (queue容器配接器)"></a>STL容器源码剖析 (queue容器配接器)</h1><p>STL在日程编程应用非常的广泛,之前看到一篇大牛文章说，用C++开发，尽量用容器类+迭代器来代替数组+指针，因为数组+指针容易越界，或者内存泄露，相反，容器类和+迭代器都有大神将底层封装好，使用安全简单。</p><p>queue容器配接器是一种先进先出的数据结构，只能在队列头部进行移除元素，在队列尾部新增元素，不能遍历容器。queue是基于某种容器作为底部结构的，默认容器是deque容器，用户也可以自己指定容器的类型。对于queue底层容器可以是deque，也可以是list，但不能是vector,map,set，使用默认的deque效率在插入方面比其他容器作为底层要快！</p><h2 id="queue容器配接器"><a href="#queue容器配接器" class="headerlink" title="queue容器配接器"></a>queue容器配接器</h2><p>由于只是在deque容器基础的接口实现，具体细节可看deque源码剖析，queue源码如下</p><pre class=" language-c++"><code class="language-c++">#ifndef __SGI_STL_INTERNAL_QUEUE_H#define __SGI_STL_INTERNAL_QUEUE_H#include <sequence_concepts.h>__STL_BEGIN_NAMESPACE// Forward declarations of operators < and ==, needed for friend declaration.//默认底层容器为deque容器template <class _Tp,           class _Sequence __STL_DEPENDENT_DEFAULT_TMPL(deque<_Tp>) >class queue;template <class _Tp, class _Seq>inline bool operator==(const queue<_Tp, _Seq>&, const queue<_Tp, _Seq>&);template <class _Tp, class _Seq>inline bool operator<(const queue<_Tp, _Seq>&, const queue<_Tp, _Seq>&);template <class _Tp, class _Sequence>class queue &#123;  // requirements:  __STL_CLASS_REQUIRES(_Tp, _Assignable);  __STL_CLASS_REQUIRES(_Sequence, _FrontInsertionSequence);  __STL_CLASS_REQUIRES(_Sequence, _BackInsertionSequence);  typedef typename _Sequence::value_type _Sequence_value_type;  __STL_CLASS_REQUIRES_SAME_TYPE(_Tp, _Sequence_value_type);#ifdef __STL_MEMBER_TEMPLATES   template <class _Tp1, class _Seq1>  friend bool operator== (const queue<_Tp1, _Seq1>&,                          const queue<_Tp1, _Seq1>&);  template <class _Tp1, class _Seq1>  friend bool operator< (const queue<_Tp1, _Seq1>&,                         const queue<_Tp1, _Seq1>&);#else /* __STL_MEMBER_TEMPLATES */  friend bool __STD_QUALIFIER  operator== __STL_NULL_TMPL_ARGS (const queue&, const queue&);  friend bool __STD_QUALIFIER  operator<  __STL_NULL_TMPL_ARGS (const queue&, const queue&);#endif /* __STL_MEMBER_TEMPLATES */public:    // queue仅支持对头部和尾部的操作, 所以不定义STL要求的    // pointer, iterator, difference_type   typedef typename _Sequence::value_type      value_type;  typedef typename _Sequence::size_type       size_type;  typedef          _Sequence                  container_type;  typedef typename _Sequence::reference       reference;  typedef typename _Sequence::const_reference const_reference;protected:  _Sequence c;//底层容器，默认为deque容器，用户可自行指定容器类型public:    //下面对queue的维护完全依赖于底层容器的操作   queue() : c() &#123;&#125;  explicit queue(const _Sequence& __c) : c(__c) &#123;&#125;  //判断容器是否为空  bool empty() const &#123; return c.empty(); &#125;  //返回容器中元素的个数  size_type size() const &#123; return c.size(); &#125;  //返回队头元素的引用  reference front() &#123; return c.front(); &#125;  const_reference front() const &#123; return c.front(); &#125;  //返回队尾元素的引用  reference back() &#123; return c.back(); &#125;  const_reference back() const &#123; return c.back(); &#125;  //只能在队尾新增元素  void push(const value_type& __x) &#123; c.push_back(__x); &#125;  //只能在队头移除元素  void pop() &#123; c.pop_front(); &#125;&#125;;//下面是依赖于底层容器的操作运算符  template <class _Tp, class _Sequence>bool operator==(const queue<_Tp, _Sequence>& __x, const queue<_Tp, _Sequence>& __y)&#123;  return __x.c == __y.c;&#125;template <class _Tp, class _Sequence>booloperator<(const queue<_Tp, _Sequence>& __x, const queue<_Tp, _Sequence>& __y)&#123;  return __x.c < __y.c;&#125;#ifdef __STL_FUNCTION_TMPL_PARTIAL_ORDERtemplate <class _Tp, class _Sequence>booloperator!=(const queue<_Tp, _Sequence>& __x, const queue<_Tp, _Sequence>& __y)&#123;  return !(__x == __y);&#125;template <class _Tp, class _Sequence>bool operator>(const queue<_Tp, _Sequence>& __x, const queue<_Tp, _Sequence>& __y)&#123;  return __y < __x;&#125;template <class _Tp, class _Sequence>bool operator<=(const queue<_Tp, _Sequence>& __x, const queue<_Tp, _Sequence>& __y)&#123;  return !(__y < __x);&#125;template <class _Tp, class _Sequence>bool operator>=(const queue<_Tp, _Sequence>& __x, const queue<_Tp, _Sequence>& __y)&#123;  return !(__x < __y);&#125;#endif /* __STL_FUNCTION_TMPL_PARTIAL_ORDER */</code></pre><p>参考资料：<br>《STL源码剖析》侯捷<br>各位大佬博客</p>]]></content>
      
      
      <categories>
          
          <category> STL </category>
          
      </categories>
      
      
        <tags>
            
            <tag> STL, C++ - Markdown </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>STL容器源码剖析 (stack容器配接器)</title>
      <link href="2021/06/01/stack/"/>
      <url>2021/06/01/stack/</url>
      
        <content type="html"><![CDATA[<h1 id="STL容器源码剖析-stack容器配接器"><a href="#STL容器源码剖析-stack容器配接器" class="headerlink" title="STL容器源码剖析 (stack容器配接器)"></a>STL容器源码剖析 (stack容器配接器)</h1><p>STL在日程编程应用非常的广泛,之前看到一篇大牛文章说，用C++开发，尽量用容器类+迭代器来代替数组+指针，因为数组+指针容易越界，或者内存泄露，相反，容器类和+迭代器都有大神将底层封装好，使用安全简单。</p><p>stack是一种先进后出的数据结构,单向开口，只能在栈顶执行push、pop和top操作，不允许遍历。stack是基于某种容器作为底部结构的，默认容器是deque容器，用户也可以自己指定容器的类型，可以是vector、deque、list，但不可以是map、set</p><h2 id="stack容器配接器"><a href="#stack容器配接器" class="headerlink" title="stack容器配接器"></a>stack容器配接器</h2><p>由于只是在deque容器基础的接口实现，具体细节可看deque源码剖析，stack源码如下</p><pre class=" language-c++"><code class="language-c++">#ifndef __SGI_STL_INTERNAL_STACK_H#define __SGI_STL_INTERNAL_STACK_H#include <sequence_concepts.h>__STL_BEGIN_NAMESPACE// Forward declarations of operators == and <, needed for friend declaration.//这里默认的底层容器类型是deque容器template <class _Tp,           class _Sequence __STL_DEPENDENT_DEFAULT_TMPL(deque<_Tp>) >class stack;template <class _Tp, class _Seq>bool operator==(const stack<_Tp,_Seq>& __x, const stack<_Tp,_Seq>& __y);template <class _Tp, class _Seq>bool operator<(const stack<_Tp,_Seq>& __x, const stack<_Tp,_Seq>& __y);template <class _Tp, class _Sequence>class stack &#123;  // requirements:  __STL_CLASS_REQUIRES(_Tp, _Assignable);  __STL_CLASS_REQUIRES(_Sequence, _BackInsertionSequence);  typedef typename _Sequence::value_type _Sequence_value_type;  __STL_CLASS_REQUIRES_SAME_TYPE(_Tp, _Sequence_value_type);#ifdef __STL_MEMBER_TEMPLATES  template <class _Tp1, class _Seq1>  friend bool operator== (const stack<_Tp1, _Seq1>&,                          const stack<_Tp1, _Seq1>&);  template <class _Tp1, class _Seq1>  friend bool operator< (const stack<_Tp1, _Seq1>&,                         const stack<_Tp1, _Seq1>&);#else /* __STL_MEMBER_TEMPLATES */  friend bool __STD_QUALIFIER  operator== __STL_NULL_TMPL_ARGS (const stack&, const stack&);  friend bool __STD_QUALIFIER  operator< __STL_NULL_TMPL_ARGS (const stack&, const stack&);#endif /* __STL_MEMBER_TEMPLATES */public:    // 由于stack仅支持对栈顶元素的操作, 所以不定义STL要求的      // pointer, iterator, difference_type   typedef typename _Sequence::value_type      value_type;  typedef typename _Sequence::size_type       size_type;  typedef          _Sequence                  container_type;  typedef typename _Sequence::reference       reference;  typedef typename _Sequence::const_reference const_reference;protected:  _Sequence c;//底层容器类型，默认为deque容器public:    //下面对stack的维护完全依赖于底层容器的操作  stack() : c() &#123;&#125;  explicit stack(const _Sequence& __s) : c(__s) &#123;&#125;  //判断容器是否为空  bool empty() const &#123; return c.empty(); &#125;  //获取容器的大小，即容器中元素的个数  size_type size() const &#123; return c.size(); &#125;  //返回栈顶元素的引用  reference top() &#123; return c.back(); &#125;  const_reference top() const &#123; return c.back(); &#125;  //在栈顶追加元素  void push(const value_type& __x) &#123; c.push_back(__x); &#125;  //弹出栈顶的元素，但不返回任何内容  void pop() &#123; c.pop_back(); &#125;&#125;;//下面是依赖于底层容器的操作运算符template <class _Tp, class _Seq>bool operator==(const stack<_Tp,_Seq>& __x, const stack<_Tp,_Seq>& __y)&#123;  return __x.c == __y.c;&#125;template <class _Tp, class _Seq>bool operator<(const stack<_Tp,_Seq>& __x, const stack<_Tp,_Seq>& __y)&#123;  return __x.c < __y.c;&#125;#ifdef __STL_FUNCTION_TMPL_PARTIAL_ORDERtemplate <class _Tp, class _Seq>bool operator!=(const stack<_Tp,_Seq>& __x, const stack<_Tp,_Seq>& __y)&#123;  return !(__x == __y);&#125;template <class _Tp, class _Seq>bool operator>(const stack<_Tp,_Seq>& __x, const stack<_Tp,_Seq>& __y)&#123;  return __y < __x;&#125;template <class _Tp, class _Seq>bool operator<=(const stack<_Tp,_Seq>& __x, const stack<_Tp,_Seq>& __y)&#123;  return !(__y < __x);&#125;template <class _Tp, class _Seq>bool operator>=(const stack<_Tp,_Seq>& __x, const stack<_Tp,_Seq>& __y)&#123;  return !(__x < __y);&#125;#endif /* __STL_FUNCTION_TMPL_PARTIAL_ORDER */__STL_END_NAMESPACE#endif /* __SGI_STL_INTERNAL_STACK_H */</code></pre><p>参考资料：<br>《STL源码剖析》侯捷<br>各位大佬博客</p>]]></content>
      
      
      <categories>
          
          <category> STL </category>
          
      </categories>
      
      
        <tags>
            
            <tag> STL, C++ - Markdown </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>STL容器源码剖析 (deque)</title>
      <link href="2021/06/01/deque/"/>
      <url>2021/06/01/deque/</url>
      
        <content type="html"><![CDATA[<h1 id="STL容器源码剖析-deque"><a href="#STL容器源码剖析-deque" class="headerlink" title="STL容器源码剖析 (deque)"></a>STL容器源码剖析 (deque)</h1><p>STL在日程编程应用非常的广泛,之前看到一篇大牛文章说，用C++开发，尽量用容器类+迭代器来代替数组+指针，因为数组+指针容易越界，或者内存泄露，相反，容器类和+迭代器都有大神将底层封装好，使用安全简单。</p><p>deque容器是一种双向开口的分段连续线性空间,可以在头尾端进行元素的插入和删除。deque由一段一段的定量连续空间构成。当需要增加新空间时，直接串接在整个deque的头端或尾端。避开了“重新配置、复制、释放”的轮回，代价则是复杂的迭代器架构。</p><p>deque采用一块所谓的map（注意，不是STL的map容器）作为主控。这里所谓map是一小块连续空间，其中每个元素（此处称为一个节点，node）都是指针，指向另一段（较大的）连续线性空间，称为缓冲区。缓冲区才是deque的储存空间主体。</p><p>对于deque容器类的详细信息可以查看<a href="https://zh.cppreference.com/w/cpp/container/deque">deque</a></p><h2 id="deque中控器"><a href="#deque中控器" class="headerlink" title="deque中控器"></a>deque中控器</h2><p>如图所示</p><pre class=" language-c++"><code class="language-c++"> _Tp**_M_map;  size_t _M_map_size;</code></pre><p><img src="https://tuchuang-1300238575.cos.ap-beijing.myqcloud.com/myblog/deque%E4%B8%AD%E6%8E%A7%E5%99%A8.png"></p><h2 id="deque迭代器"><a href="#deque迭代器" class="headerlink" title="deque迭代器"></a>deque迭代器</h2><p>deuqe迭代器必须知道缓冲区的位置，能够判断是否处于其所在缓冲区的边界，能够知道其所在缓冲区当前所指位置的元素，其数据结构如下：</p><pre class=" language-c++"><code class="language-c++">struct _Deque_iterator &#123;    ...   //以下是迭代器设计的关键，访问容器的节点  _Tp* _M_cur;//指向缓冲区当前的元素  _Tp* _M_first;//指向缓冲区的头(起始地址)  _Tp* _M_last;//指向缓冲区的尾(结束地址)  _Map_pointer _M_node;//指向中控器的相应节点  ...&#125;;</code></pre><p>此外，迭代器和中控器以及缓冲区的关系如下图<br><img src="https://tuchuang-1300238575.cos.ap-beijing.myqcloud.com/myblog/deque.png"></p><p>迭代器完整源码如下</p><pre class=" language-c++"><code class="language-c++">//*    deque是分段连续线性空间，为了能够像连续线性空间那样访问数据，//*    deque采用了一种中控方法map,map是块连续的空间，其每个元素都是一个指针，//*    指向一块缓冲区，实质上map是T**；//*********************************//这个函数是方便不同编译器处理常量表达式的buginline size_t __deque_buf_size(size_t __size) &#123;  return __size < 512 ? size_t(512 / __size) : size_t(1);&#125;template <class _Tp, class _Ref, class _Ptr>struct _Deque_iterator &#123;  typedef _Deque_iterator<_Tp, _Tp&, _Tp*>             iterator;  typedef _Deque_iterator<_Tp, const _Tp&, const _Tp*> const_iterator;  static size_t _S_buffer_size() &#123; return __deque_buf_size(sizeof(_Tp)); &#125;  typedef random_access_iterator_tag iterator_category;  typedef _Tp value_type;  typedef _Ptr pointer;  typedef _Ref reference;  typedef size_t size_type;  typedef ptrdiff_t difference_type;  typedef _Tp**_Map_pointer;  typedef _Deque_iterator _Self;  //以下是迭代器设计的关键，访问容器的节点  _Tp* _M_cur;//指向缓冲区当前的元素  _Tp* _M_first;//指向缓冲区的头(起始地址)  _Tp* _M_last;//指向缓冲区的尾(结束地址)  _Map_pointer _M_node;//指向中控器的相应节点  _Deque_iterator(_Tp* __x, _Map_pointer __y)     : _M_cur(__x), _M_first(*__y),      _M_last(*__y + _S_buffer_size()), _M_node(__y) &#123;&#125;  _Deque_iterator() : _M_cur(0), _M_first(0), _M_last(0), _M_node(0) &#123;&#125;  _Deque_iterator(const iterator& __x)    : _M_cur(__x._M_cur), _M_first(__x._M_first),       _M_last(__x._M_last), _M_node(__x._M_node) &#123;&#125;  //**************************************  //***********以下是迭代器_Deque_iterator的操作************  //*    这些操作符的重载方便我们访问容器的内容  //*    也是让deque从接口看来是维护连续线性空间的关键  //**************************************  reference operator*() const &#123; return *_M_cur; &#125;//解除引用,返回当前元素#ifndef __SGI_STL_NO_ARROW_OPERATOR  pointer operator->() const &#123; return _M_cur; &#125;#endif /* __SGI_STL_NO_ARROW_OPERATOR */  //返回两个迭代器之间的距离  difference_type operator-(const _Self& __x) const &#123;    return difference_type(_S_buffer_size()) * (_M_node - __x._M_node - 1) +      (_M_cur - _M_first) + (__x._M_last - __x._M_cur);  &#125;  //前缀自增++操作符重载  _Self& operator++() &#123;    ++_M_cur;                    //普通自增操作，移至下一个元素    if (_M_cur == _M_last) &#123;   //若已达到缓冲区的尾部      _M_set_node(_M_node + 1);//切换至下一缓冲区(节点)      _M_cur = _M_first;      //的第一个元素    &#125;    return *this;  &#125;  //后缀自增++操作符重载  //返回当前迭代器的一个副本  _Self operator++(int)  &#123;    _Self __tmp = *this;//定义当前迭代器的一个副本    ++*this;//这里前缀++不是普通的++操作，是上一步骤已经重载过的前缀++    return __tmp;  &#125;  //前缀自减--操作符重载  //基本思想类似于前缀自增操作  _Self& operator--() &#123;    if (_M_cur == _M_first) &#123;  //若是当前缓冲区的第一个元素      _M_set_node(_M_node - 1);//切换到上一个缓冲区      _M_cur = _M_last;          //的尾部(即最后一个元素的下一个位置)    &#125;    --_M_cur;//普通的自减操作，移至前一个元素    return *this;  &#125;  //后缀自减--操作符重载  //返回当前迭代器的副本  _Self operator--(int) &#123;    _Self __tmp = *this;//定义一个副本    --*this;            //迭代器自减操作    return __tmp;  &#125;  //以下实现随机存取，迭代器可以直接跳跃n个距离  //将迭代器前移n个距离，当n负值时就为下面的operator-=操作  _Self& operator+=(difference_type __n)  &#123;    difference_type __offset = __n + (_M_cur - _M_first);//定义一个中间变量    if (__offset >= 0 && __offset < difference_type(_S_buffer_size()))        //若前移n个距离后，目标依然在同一个缓冲区        //则直接前移n个距离      _M_cur += __n;    else &#123;        //若前移n个距离后，目标超出该缓冲区范围        //__offset / difference_type(_S_buffer_size())计算向后移动多少个缓冲区        //-difference_type((-__offset - 1) / _S_buffer_size()) - 1计算向前移动多少个缓冲区      difference_type __node_offset =        __offset > 0 ? __offset / difference_type(_S_buffer_size())                   : -difference_type((-__offset - 1) / _S_buffer_size()) - 1;      //调整到正确的缓冲区      _M_set_node(_M_node + __node_offset);      //切换至正确的元素      _M_cur = _M_first +         (__offset - __node_offset * difference_type(_S_buffer_size()));    &#125;    return *this;  &#125;  //操作符+重载  //返回操作之后的副本  _Self operator+(difference_type __n) const  &#123;    _Self __tmp = *this;    //调用operator+=操作    return __tmp += __n;  &#125;  //利用operator+=操作实现  _Self& operator-=(difference_type __n) &#123; return *this += -__n; &#125;   _Self operator-(difference_type __n) const &#123;    _Self __tmp = *this;    return __tmp -= __n;  &#125;  //返回指定位置的元素，即实现随机存取  //该函数调用operator+，operator*  reference operator[](difference_type __n) const &#123; return *(*this + __n); &#125;  bool operator==(const _Self& __x) const &#123; return _M_cur == __x._M_cur; &#125;  bool operator!=(const _Self& __x) const &#123; return !(*this == __x); &#125;  bool operator<(const _Self& __x) const &#123;    return (_M_node == __x._M_node) ?       (_M_cur < __x._M_cur) : (_M_node < __x._M_node);  &#125;  bool operator>(const _Self& __x) const  &#123; return __x < *this; &#125;  bool operator<=(const _Self& __x) const &#123; return !(__x < *this); &#125;  bool operator>=(const _Self& __x) const &#123; return !(*this < __x); &#125;  //调整到正确的缓冲区  //切换到正确的元素位置  void _M_set_node(_Map_pointer __new_node) &#123;    _M_node = __new_node;//指向新的节点    _M_first = *__new_node;//指向新节点的头部    _M_last = _M_first + difference_type(_S_buffer_size());//指向新节点的尾部  &#125;&#125;;</code></pre><h2 id="deque容器的构造函数和析构函数"><a href="#deque容器的构造函数和析构函数" class="headerlink" title="deque容器的构造函数和析构函数"></a>deque容器的构造函数和析构函数</h2><p>源码如下：</p><pre class=" language-c++"><code class="language-c++">public:                             //******************************    //*    以下是构造函数和析构函数    //*    默认构造函数    //*    explicit deque( const Allocator& alloc = Allocator() );    //*    容器大小为count个初始值为value的元素    //*    explicit deque( size_type count,    //*            const T& value = T(),    //*            const Allocator& alloc = Allocator());    //*     deque( size_type count,    //*            const T& value,    //*            const Allocator& alloc = Allocator());    //*    容器大小为count个默认值的元素    //*    explicit deque( size_type count );    //*    拷贝构造函数    //*    deque( const deque& other );    //*    deque( const deque& other, const Allocator& alloc );    //*    初始值为[first,last)内容的容器    //*    template< class InputIt >    //*    deque( InputIt first, InputIt last,    //*            const Allocator& alloc = Allocator() );        //*    析构函数    //*    ~deque()    //*    //****************************                                    // Constructor, destructor.  explicit deque(const allocator_type& __a = allocator_type())     : _Base(__a, 0) &#123;&#125;  deque(const deque& __x) : _Base(__x.get_allocator(), __x.size())     &#123; uninitialized_copy(__x.begin(), __x.end(), _M_start); &#125;  deque(size_type __n, const value_type& __value,        const allocator_type& __a = allocator_type()) : _Base(__a, __n)    &#123; _M_fill_initialize(__value); &#125;  explicit deque(size_type __n) : _Base(allocator_type(), __n)    &#123; _M_fill_initialize(value_type()); &#125;#ifdef __STL_MEMBER_TEMPLATES  // Check whether it's an integral type.  If so, it's not an iterator.  template <class _InputIterator>  deque(_InputIterator __first, _InputIterator __last,        const allocator_type& __a = allocator_type()) : _Base(__a) &#123;    typedef typename _Is_integer<_InputIterator>::_Integral _Integral;    _M_initialize_dispatch(__first, __last, _Integral());  &#125;  template <class _Integer>  void _M_initialize_dispatch(_Integer __n, _Integer __x, __true_type) &#123;    _M_initialize_map(__n);    _M_fill_initialize(__x);  &#125;  template <class _InputIter>  void _M_initialize_dispatch(_InputIter __first, _InputIter __last,                              __false_type) &#123;    _M_range_initialize(__first, __last, __ITERATOR_CATEGORY(__first));  &#125;#else /* __STL_MEMBER_TEMPLATES */  deque(const value_type* __first, const value_type* __last,        const allocator_type& __a = allocator_type())     : _Base(__a, __last - __first)    &#123; uninitialized_copy(__first, __last, _M_start); &#125;  deque(const_iterator __first, const_iterator __last,        const allocator_type& __a = allocator_type())     : _Base(__a, __last - __first)    &#123; uninitialized_copy(__first, __last, _M_start); &#125;#endif /* __STL_MEMBER_TEMPLATES */  ~deque() &#123; destroy(_M_start, _M_finish); &#125;</code></pre><h2 id="deque容器的成员函数"><a href="#deque容器的成员函数" class="headerlink" title="deque容器的成员函数"></a>deque容器的成员函数</h2><p>源码如下</p><pre class=" language-c++"><code class="language-c++"> //交换两个容器的内容  //这里实际上并没有交换具体的元素，而是交换迭代器  //和中控器信息  void swap(deque& __x) &#123;    __STD::swap(_M_start, __x._M_start);    __STD::swap(_M_finish, __x._M_finish);    __STD::swap(_M_map, __x._M_map);    __STD::swap(_M_map_size, __x._M_map_size);  &#125;public:   void _M_fill_assign(size_type __n, const _Tp& __val) &#123;    if (__n > size()) &#123;//若新的大小比容器原来的大      fill(begin(), end(), __val);//把原来容器填充为val      insert(end(), __n - size(), __val);//容器追加填充值    &#125;    else &#123;//若新的大小比容器原来的小      erase(begin() + __n, end());//则擦除多余的内容      fill(begin(), end(), __val);//填充值为val覆盖原来的内容    &#125;  &#125;  //对外接口的assign第一个版本  void assign(size_type __n, const _Tp& __val) &#123;    _M_fill_assign(__n, __val);  &#125;#ifdef __STL_MEMBER_TEMPLATES  //对外接口的assign第二个版本  //采用迭代器的traist技术  template <class _InputIterator>  void assign(_InputIterator __first, _InputIterator __last) &#123;    typedef typename _Is_integer<_InputIterator>::_Integral _Integral;    _M_assign_dispatch(__first, __last, _Integral());  &#125;private:                          template <class _Integer>  void _M_assign_dispatch(_Integer __n, _Integer __val, __true_type)    &#123; _M_fill_assign((size_type) __n, (_Tp) __val); &#125;  template <class _InputIterator>  void _M_assign_dispatch(_InputIterator __first, _InputIterator __last,                          __false_type) &#123;    _M_assign_aux(__first, __last, __ITERATOR_CATEGORY(__first));  &#125;  template <class _InputIterator>  void _M_assign_aux(_InputIterator __first, _InputIterator __last,                     input_iterator_tag);  template <class _ForwardIterator>  void _M_assign_aux(_ForwardIterator __first, _ForwardIterator __last,                     forward_iterator_tag) &#123;    size_type __len = 0;    distance(__first, __last, __len);//计算两个输入迭代器的距离    if (__len > size()) &#123;//若输入迭代器距离比待赋值容器大小还大      _ForwardIterator __mid = __first;      advance(__mid, size());//将mid向前移动size()个距离      copy(__first, __mid, begin());//先复制[first,mid)数据      insert(end(), __mid, __last);//插入剩下的数据    &#125;    else//否则擦除多余的数据      erase(copy(__first, __last, begin()), end());  &#125;#endif /* __STL_MEMBER_TEMPLATES */public:                         // push_* and pop_*    //在容器尾部加数据    void push_back(const value_type& __t) &#123;    //若当前缓冲区存在可用空间    if (_M_finish._M_cur != _M_finish._M_last - 1) &#123;      construct(_M_finish._M_cur, __t);//直接构造对象      ++_M_finish._M_cur;//调整指针所指位置    &#125;    else//若当前缓冲区不存在可用空间        //需分配一段新的连续空间      _M_push_back_aux(__t);  &#125;  void push_back() &#123;    if (_M_finish._M_cur != _M_finish._M_last - 1) &#123;      construct(_M_finish._M_cur);      ++_M_finish._M_cur;    &#125;    else      _M_push_back_aux();  &#125;  void push_front(const value_type& __t) &#123;    if (_M_start._M_cur != _M_start._M_first) &#123;      construct(_M_start._M_cur - 1, __t);      --_M_start._M_cur;    &#125;    else      _M_push_front_aux(__t);  &#125;  void push_front() &#123;    if (_M_start._M_cur != _M_start._M_first) &#123;      construct(_M_start._M_cur - 1);      --_M_start._M_cur;    &#125;    else      _M_push_front_aux();  &#125;  void pop_back() &#123;    if (_M_finish._M_cur != _M_finish._M_first) &#123;      --_M_finish._M_cur;      destroy(_M_finish._M_cur);    &#125;    else      _M_pop_back_aux();  &#125;  void pop_front() &#123;    if (_M_start._M_cur != _M_start._M_last - 1) &#123;      destroy(_M_start._M_cur);      ++_M_start._M_cur;    &#125;    else       _M_pop_front_aux();  &#125;public:                 //*******************************    /*    在指定位置之前插入数据    *******************************/  iterator insert(iterator position, const value_type& __x) &#123;    if (position._M_cur == _M_start._M_cur) &#123;//若当前位置为deque.begin()      push_front(__x);//则在容器头部插入数据      return _M_start;    &#125;    else if (position._M_cur == _M_finish._M_cur) &#123;//若当前位置为deque.end()      push_back(__x);      iterator __tmp = _M_finish;      --__tmp;      return __tmp;    &#125;    else &#123;//否则在容器直接插入数据      return _M_insert_aux(position, __x);    &#125;  &#125;  iterator insert(iterator __position)    &#123; return insert(__position, value_type()); &#125;  void insert(iterator __pos, size_type __n, const value_type& __x)    &#123; _M_fill_insert(__pos, __n, __x); &#125;  void _M_fill_insert(iterator __pos, size_type __n, const value_type& __x); #ifdef __STL_MEMBER_TEMPLATES    // Check whether it's an integral type.  If so, it's not an iterator.  template <class _InputIterator>  void insert(iterator __pos, _InputIterator __first, _InputIterator __last) &#123;    typedef typename _Is_integer<_InputIterator>::_Integral _Integral;    _M_insert_dispatch(__pos, __first, __last, _Integral());  &#125;  template <class _Integer>  void _M_insert_dispatch(iterator __pos, _Integer __n, _Integer __x,                          __true_type) &#123;    _M_fill_insert(__pos, (size_type) __n, (value_type) __x);  &#125;  template <class _InputIterator>  void _M_insert_dispatch(iterator __pos,                          _InputIterator __first, _InputIterator __last,                          __false_type) &#123;    insert(__pos, __first, __last, __ITERATOR_CATEGORY(__first));  &#125;#else /* __STL_MEMBER_TEMPLATES */  void insert(iterator __pos,              const value_type* __first, const value_type* __last);  void insert(iterator __pos,              const_iterator __first, const_iterator __last);#endif /* __STL_MEMBER_TEMPLATES */  //改变容器大小  void resize(size_type __new_size, const value_type& __x) &#123;    const size_type __len = size();    if (__new_size < __len)//新的大小较旧的小       erase(_M_start + __new_size, _M_finish);//擦除多余的元素    else//否则，填充新增的节点      insert(_M_finish, __new_size - __len, __x);  &#125;  void resize(size_type new_size) &#123; resize(new_size, value_type()); &#125;public:                  /*    擦除指定位置的元素        *    iterator erase (iterator position);        *    擦除指定区间的元素        *    iterator erase (iterator first, iterator last);        */                                          iterator erase(iterator __pos) &#123;    iterator __next = __pos;    ++__next;    difference_type __index = __pos - _M_start;//擦除点之前元素个数    if (size_type(__index) < (this->size() >> 1)) &#123;//若擦除点之前的元素个数较少      copy_backward(_M_start, __pos, __next);//向后移动擦除点之前的元素      pop_front();    &#125;    else &#123;      copy(__next, _M_finish, __pos);//否则，向前移动擦除点之后的元素      pop_back();    &#125;    return _M_start + __index;  &#125;  iterator erase(iterator __first, iterator __last);  void clear(); protected:                        // Internal construction/destruction  void _M_fill_initialize(const value_type& __value);#ifdef __STL_MEMBER_TEMPLATES    template <class _InputIterator>  void _M_range_initialize(_InputIterator __first, _InputIterator __last,                        input_iterator_tag);  template <class _ForwardIterator>  void _M_range_initialize(_ForwardIterator __first, _ForwardIterator __last,                        forward_iterator_tag);#endif /* __STL_MEMBER_TEMPLATES */protected:                        // Internal push_* and pop_*  void _M_push_back_aux(const value_type&);  void _M_push_back_aux();  void _M_push_front_aux(const value_type&);  void _M_push_front_aux();  void _M_pop_back_aux();  void _M_pop_front_aux();protected:                        // Internal insert functions#ifdef __STL_MEMBER_TEMPLATES    template <class _InputIterator>  void insert(iterator __pos, _InputIterator __first, _InputIterator __last,              input_iterator_tag);  template <class _ForwardIterator>  void insert(iterator __pos,              _ForwardIterator __first, _ForwardIterator __last,              forward_iterator_tag);#endif /* __STL_MEMBER_TEMPLATES */  iterator _M_insert_aux(iterator __pos, const value_type& __x);  iterator _M_insert_aux(iterator __pos);  void _M_insert_aux(iterator __pos, size_type __n, const value_type& __x);#ifdef __STL_MEMBER_TEMPLATES    template <class _ForwardIterator>  void _M_insert_aux(iterator __pos,                      _ForwardIterator __first, _ForwardIterator __last,                     size_type __n);#else /* __STL_MEMBER_TEMPLATES */    void _M_insert_aux(iterator __pos,                     const value_type* __first, const value_type* __last,                     size_type __n);  void _M_insert_aux(iterator __pos,                      const_iterator __first, const_iterator __last,                     size_type __n); #endif /* __STL_MEMBER_TEMPLATES */  iterator _M_reserve_elements_at_front(size_type __n) &#123;    size_type __vacancies = _M_start._M_cur - _M_start._M_first;    if (__n > __vacancies)       _M_new_elements_at_front(__n - __vacancies);    return _M_start - difference_type(__n);  &#125;  iterator _M_reserve_elements_at_back(size_type __n) &#123;    size_type __vacancies = (_M_finish._M_last - _M_finish._M_cur) - 1;    if (__n > __vacancies)      _M_new_elements_at_back(__n - __vacancies);    return _M_finish + difference_type(__n);  &#125;  void _M_new_elements_at_front(size_type __new_elements);  void _M_new_elements_at_back(size_type __new_elements);protected:                     void _M_reserve_map_at_back (size_type __nodes_to_add = 1) &#123;      //_M_map_size - (_M_finish._M_node - _M_map)表示map尾端节点的备用空间    if (__nodes_to_add + 1 > _M_map_size - (_M_finish._M_node - _M_map))      _M_reallocate_map(__nodes_to_add, false);  &#125;  void _M_reserve_map_at_front (size_type __nodes_to_add = 1) &#123;       //_M_start._M_node - _M_map表示map头部节点的备用空间    if (__nodes_to_add > size_type(_M_start._M_node - _M_map))      _M_reallocate_map(__nodes_to_add, true);  &#125;  void _M_reallocate_map(size_type __nodes_to_add, bool __add_at_front);&#125;;</code></pre><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul><li><p>deque的erase函数它会根据具体位置来选择是往前移动还是往后移动，其他的函数 类似pop push其实和之前vector和list差不多。具体先判断空间够不够 是不是指向当前缓冲区的first|last 等一系列问题，如果空间不够 就申请新的缓冲区 然后map指针指向下一个map数组位置， 然后再对应指向新的缓冲区 顺便更新迭代器start end cur</p></li><li><p>为什么STL的 stack 默认使用deque而不是vector作为底层容器——随着容器的元素增加，对于vector而言可能涉及到申请新的空间，复制原有的元素到新的空间，释放原有的旧空间。而deque则没有这个问题。</p></li></ul><p>参考资料：<br>《STL源码剖析》侯捷<br>各位大佬博客</p>]]></content>
      
      
      <categories>
          
          <category> STL </category>
          
      </categories>
      
      
        <tags>
            
            <tag> STL, C++ - Markdown </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>STL容器源码剖析 (list)</title>
      <link href="2021/05/31/list/"/>
      <url>2021/05/31/list/</url>
      
        <content type="html"><![CDATA[<h1 id="STL容器源码剖析-list"><a href="#STL容器源码剖析-list" class="headerlink" title="STL容器源码剖析 (list)"></a>STL容器源码剖析 (list)</h1><p>STL在日程编程应用非常的广泛,之前看到一篇大牛文章说，用C++开发，尽量用容器类+迭代器来代替数组+指针，因为数组+指针容易越界，或者内存泄露，相反，容器类和+迭代器都有大神将底层封装好，使用安全简单。</p><p>list容器是一个循环的双向链表,list容器的内存空间不一定是连续存储，内存之间是采用迭代器或节点指针进行连接，并且在插入或删除数据节点时，就配置或释放一个数据节点，并不会分配额外的内存空间。</p><p>与vector容器不同的是，list容器在进行插入操作或拼接操作时，迭代器并不会失效；且不能以普通指针作为迭代器，因为普通指针的+或-操作只能指向连续空间的后移地址或前移个地址，不能保证指向list的下一个节点，迭代器必须是双向迭代器，因为list容器具备有前移和后移的能力。对于list容器类的详细信息可以查看<a href="https://zh.cppreference.com/w/cpp/container/list">list</a></p><h2 id="list节点和list数据结构"><a href="#list节点和list数据结构" class="headerlink" title="list节点和list数据结构"></a>list节点和list数据结构</h2><p>在list容器中，list本身和list节点是分开设计的，list节点结构源码如下：</p><pre class=" language-c++"><code class="language-c++">struct _List_node_base &#123;  _List_node_base* _M_next;//后继节点  _List_node_base* _M_prev;//前驱节点&#125;;template <class _Tp>struct _List_node : public _List_node_base &#123;  _Tp _M_data;//节点存储的数据&#125;;</code></pre><p>list容器本身是循环双向链表，源码如下：</p><pre class=" language-c++"><code class="language-c++">//以下是双向链表list类的定义，分配器_Alloc默认为第二级配置器template <class _Tp, class _Alloc = __STL_DEFAULT_ALLOCATOR(_Tp) >class list : protected _List_base<_Tp, _Alloc> &#123;      ...public:    typedef _List_node<_Tp> _Node;protected:    //指向链表节点指针    _List_node<_Tp>* _M_node;    ...&#125;;</code></pre><h2 id="list容器的迭代器"><a href="#list容器的迭代器" class="headerlink" title="list容器的迭代器"></a>list容器的迭代器</h2><p>list容器的迭代器是双向迭代器，这也是导致list容器的排序成员函数sort()不能使用STL算法中的排序函数，因为STL中的排序算法接受的迭代器是随机访问迭代器；list容器在进行插入和拼接操作时迭代器不会失效,源码如下：</p><pre class=" language-c++"><code class="language-c++">//链表List_iterator_base的迭代器struct _List_iterator_base &#123;    //数据类型  typedef size_t                     size_type;  typedef ptrdiff_t                  difference_type;  //list迭代器的类型是双向迭代器bidirectional_iterator  typedef bidirectional_iterator_tag iterator_category;  //定义指向链表节点的指针  _List_node_base* _M_node;  //构造函数  _List_iterator_base(_List_node_base* __x) : _M_node(__x) &#123;&#125;  _List_iterator_base() &#123;&#125;  //更新节点指针，指向直接前驱或直接后继节点  void _M_incr() &#123; _M_node = _M_node->_M_next; &#125;  void _M_decr() &#123; _M_node = _M_node->_M_prev; &#125;  //操作符重载  bool operator==(const _List_iterator_base& __x) const &#123;    return _M_node == __x._M_node;  &#125;  bool operator!=(const _List_iterator_base& __x) const &#123;    return _M_node != __x._M_node;  &#125;&#125;;  //链表List_iterator的迭代器template<class _Tp, class _Ref, class _Ptr>struct _List_iterator : public _List_iterator_base &#123;  typedef _List_iterator<_Tp,_Tp&,_Tp*>             iterator;  typedef _List_iterator<_Tp,const _Tp&,const _Tp*> const_iterator;  typedef _List_iterator<_Tp,_Ref,_Ptr>             _Self;  typedef _Tp value_type;  typedef _Ptr pointer;  typedef _Ref reference;  typedef _List_node<_Tp> _Node;  //构造函数  _List_iterator(_Node* __x) : _List_iterator_base(__x) &#123;&#125;  _List_iterator() &#123;&#125;  _List_iterator(const iterator& __x) : _List_iterator_base(__x._M_node) &#123;&#125;  //基本操作符的重载,取出节点数据  reference operator*() const &#123; return ((_Node*) _M_node)->_M_data; &#125;#ifndef __SGI_STL_NO_ARROW_OPERATOR  pointer operator->() const &#123; return &(operator*()); &#125;#endif /* __SGI_STL_NO_ARROW_OPERATOR */  _Self& operator++() &#123;     this->_M_incr();    return *this;  &#125;  _Self operator++(int) &#123;     _Self __tmp = *this;    this->_M_incr();    return __tmp;  &#125;  _Self& operator--() &#123;     this->_M_decr();    return *this;  &#125;  _Self operator--(int) &#123;     _Self __tmp = *this;    this->_M_decr();    return __tmp;  &#125;&#125;;#ifndef __STL_CLASS_PARTIAL_SPECIALIZATION//返回迭代器的类型inline bidirectional_iterator_tagiterator_category(const _List_iterator_base&)&#123;  return bidirectional_iterator_tag();&#125;template <class _Tp, class _Ref, class _Ptr>inline _Tp*value_type(const _List_iterator<_Tp, _Ref, _Ptr>&)&#123;  return 0;&#125;inline ptrdiff_t*distance_type(const _List_iterator_base&)&#123;  return 0;&#125;#endif /* __STL_CLASS_PARTIAL_SPECIALIZATION */</code></pre><h2 id="list容器的构造函数和析构函数"><a href="#list容器的构造函数和析构函数" class="headerlink" title="list容器的构造函数和析构函数"></a>list容器的构造函数和析构函数</h2><p>我们平时定义list对象是会用到，如 list<int> list等等</p><pre class=" language-c++"><code class="language-c++">//双向链表list类的定义，分配器_Alloc默认为第二级配置器template <class _Tp, class _Alloc = __STL_DEFAULT_ALLOCATOR(_Tp) >class list : protected _List_base<_Tp, _Alloc> &#123;      ...public: //************************************  /*************以下是构造函数******************  //***********默认构造函数*********************    explicit list( const Allocator& alloc = Allocator() );  //************具有初值和大小的构造函数************    explicit list( size_type count,               const T& value = T(),               const Allocator& alloc = Allocator());         list( size_type count,               const T& value,               const Allocator& alloc = Allocator());  //********只有大小的构造函数********************    explicit list( size_type count );  //******某个范围的值为初始值的构造函数**************    template< class InputIt >    list( InputIt first, InputIt last,      const Allocator& alloc = Allocator() ); //******拷贝构造函数*************************    list( const list& other );  */  //************************************  //构造函数  //链表的默认构造函数  explicit list(const allocator_type& __a = allocator_type()) : _Base(__a) &#123;&#125;  list(size_type __n, const _Tp& __value,       const allocator_type& __a = allocator_type())    : _Base(__a)    &#123; insert(begin(), __n, __value); &#125;  explicit list(size_type __n)    : _Base(allocator_type())    &#123; insert(begin(), __n, _Tp()); &#125;#ifdef __STL_MEMBER_TEMPLATES    //我们在这里不需要任何调用技巧，因为insert可以完成所有  template <class _InputIterator>  list(_InputIterator __first, _InputIterator __last,       const allocator_type& __a = allocator_type())    : _Base(__a)    &#123; insert(begin(), __first, __last); &#125;#else /* __STL_MEMBER_TEMPLATES */  list(const _Tp* __first, const _Tp* __last,       const allocator_type& __a = allocator_type())    : _Base(__a)    &#123; this->insert(begin(), __first, __last); &#125;  list(const_iterator __first, const_iterator __last,       const allocator_type& __a = allocator_type())    : _Base(__a)    &#123; this->insert(begin(), __first, __last); &#125;#endif /* __STL_MEMBER_TEMPLATES */  list(const list<_Tp, _Alloc>& __x) : _Base(__x.get_allocator())    &#123; insert(begin(), __x.begin(), __x.end()); &#125;//拷贝构造函数  ~list() &#123; &#125;//析构函数  //赋值操作  list<_Tp, _Alloc>& operator=(const list<_Tp, _Alloc>& __x);  //***********************************    ...&#125;;list容器的成员函数---如插入insert()、擦除erase()、拼接splice()等等```c++//双向链表list类的定义，分配器_Alloc默认为第二级配置器template <class _Tp, class _Alloc = __STL_DEFAULT_ALLOCATOR(_Tp) >class list : protected _List_base<_Tp, _Alloc> &#123;  // requirements:    ...protected:    //创建值为x的节点，并返回该节点的地址  _Node* _M_create_node(const _Tp& __x)  &#123;    _Node* __p = _M_get_node();//分配一个节点空间    __STL_TRY &#123;//把x值赋予指定的地址，即是data值      _Construct(&__p->_M_data, __x);    &#125;    __STL_UNWIND(_M_put_node(__p));    return __p;//返回节点地址  &#125;  //创建默认值的节点  _Node* _M_create_node()  &#123;    _Node* __p = _M_get_node();    __STL_TRY &#123;      _Construct(&__p->_M_data);    &#125;    __STL_UNWIND(_M_put_node(__p));    return __p;  &#125;public:      //以下是迭代器的定义  iterator begin()             &#123; return (_Node*)(_M_node->_M_next); &#125;  const_iterator begin() const &#123; return (_Node*)(_M_node->_M_next); &#125;  iterator end()             &#123; return _M_node; &#125;  const_iterator end() const &#123; return _M_node; &#125;  reverse_iterator rbegin()     &#123; return reverse_iterator(end()); &#125;  const_reverse_iterator rbegin() const     &#123; return const_reverse_iterator(end()); &#125;  reverse_iterator rend()    &#123; return reverse_iterator(begin()); &#125;  const_reverse_iterator rend() const    &#123; return const_reverse_iterator(begin()); &#125;  //判断链表是否为空链表  bool empty() const &#123; return _M_node->_M_next == _M_node; &#125;   //返回链表的大小  size_type size() const &#123;    size_type __result = 0;    //返回两个迭代器之间的距离    distance(begin(), end(), __result);    //返回链表的元素个数    return __result;  &#125;  size_type max_size() const &#123; return size_type(-1); &#125;  //返回第一个节点数据的引用，reference相当于value_type&  reference front() &#123; return *begin(); &#125;  const_reference front() const &#123; return *begin(); &#125;  //返回最后一个节点数据的引用  reference back() &#123; return *(--end()); &#125;  const_reference back() const &#123; return *(--end()); &#125;  //交换链表容器的内容  void swap(list<_Tp, _Alloc>& __x) &#123; __STD::swap(_M_node, __x._M_node); &#125; //************************************ //***********插入节点*********************  /**********以下是插入节点函数的原型，也是公共接口********    //在指定的位置pos之前插入值为value的数据节点    iterator insert( iterator pos, const T& value );    iterator insert( const_iterator pos, const T& value );    //在指定的位置pos之前插入n个值为value的数据节点    void insert( iterator pos, size_type count, const T& value );    iterator insert( const_iterator pos, size_type count, const T& value );    //在指定的位置pos之前插入[first,last)之间的数据节点    template< class InputIt >    void insert( iterator pos, InputIt first, InputIt last);    template< class InputIt >    iterator insert( const_iterator pos, InputIt first, InputIt last );  *************************************/  /**在整个链表的操作中，插入操作是非常重要的，很多成员函数会调用该函数**///*************************************  //在指定的位置插入初始值为x的节点  iterator insert(iterator __position, const _Tp& __x) &#123;      //首先创建一个初始值为x的节点，并返回该节点的地址    _Node* __tmp = _M_create_node(__x);    //调整节点指针，把新节点插入到指定位置    __tmp->_M_next = __position._M_node;    __tmp->_M_prev = __position._M_node->_M_prev;    __position._M_node->_M_prev->_M_next = __tmp;    __position._M_node->_M_prev = __tmp;    //返回新节点地址    return __tmp;  &#125;  //在指定的位置插入为默认值的节点  iterator insert(iterator __position) &#123; return insert(__position, _Tp()); &#125;  //在指定位置插入n个初始值为x的节点  void insert(iterator __pos, size_type __n, const _Tp& __x)    &#123; _M_fill_insert(__pos, __n, __x); &#125;  void _M_fill_insert(iterator __pos, size_type __n, const _Tp& __x); #ifdef __STL_MEMBER_TEMPLATES  // Check whether it's an integral type.  If so, it's not an iterator.  //这里采用__type_traits技术    //在指定位置插入指定范围内的数据  //首先判断输入迭代器类型_InputIterator是否为整数类型  template <class _InputIterator>  void insert(iterator __pos, _InputIterator __first, _InputIterator __last) &#123;    typedef typename _Is_integer<_InputIterator>::_Integral _Integral;    _M_insert_dispatch(__pos, __first, __last, _Integral());  &#125;    //若输入迭代器类型_InputIterator是为整数类型，调用此函数  template<class _Integer>  void _M_insert_dispatch(iterator __pos, _Integer __n, _Integer __x,                          __true_type) &#123;    _M_fill_insert(__pos, (size_type) __n, (_Tp) __x);  &#125;  //若输入迭代器类型_InputIterator是不为整数类型，调用此函数  template <class _InputIterator>  void _M_insert_dispatch(iterator __pos,                          _InputIterator __first, _InputIterator __last,                          __false_type); #else /* __STL_MEMBER_TEMPLATES */  void insert(iterator __position, const _Tp* __first, const _Tp* __last);  void insert(iterator __position,              const_iterator __first, const_iterator __last);#endif /* __STL_MEMBER_TEMPLATES */    //在链表头插入节点  void push_front(const _Tp& __x) &#123; insert(begin(), __x); &#125;  void push_front() &#123;insert(begin());&#125;  //在链表尾插入节点  void push_back(const _Tp& __x) &#123; insert(end(), __x); &#125;  void push_back() &#123;insert(end());&#125;  //*******************************  //**********在指定位置删除节点***********  //**********以下是删除节点的公共接口*********  /******************************    //删除指定位置pos的节点    iterator erase( iterator pos );    iterator erase( const_iterator pos );    //删除指定范围[first,last)的数据节点    iterator erase( iterator first, iterator last );    iterator erase( const_iterator first, const_iterator last );  ******************************/  //*******************************  //在指定位置position删除节点，并返回直接后继节点的地址  iterator erase(iterator __position) &#123;      //调整前驱和后继节点的位置    _List_node_base* __next_node = __position._M_node->_M_next;    _List_node_base* __prev_node = __position._M_node->_M_prev;    _Node* __n = (_Node*) __position._M_node;    __prev_node->_M_next = __next_node;    __next_node->_M_prev = __prev_node;    _Destroy(&__n->_M_data);    _M_put_node(__n);    return iterator((_Node*) __next_node);  &#125;  //删除两个迭代器之间的节点  iterator erase(iterator __first, iterator __last);  //清空链表，这里是调用父类的clear()函数  void clear() &#123; _Base::clear(); &#125;  //调整链表的大小  void resize(size_type __new_size, const _Tp& __x);  void resize(size_type __new_size) &#123; this->resize(__new_size, _Tp()); &#125;  //取出第一个数据节点  void pop_front() &#123; erase(begin()); &#125;  //取出最后一个数据节点  void pop_back() &#123;     iterator __tmp = end();    erase(--__tmp);  &#125;public:  // assign(), a generalized assignment member function.  Two  // versions: one that takes a count, and one that takes a range.  // The range version is a member template, so we dispatch on whether  // or not the type is an integer.  /***********************************  //assign()函数的两个版本原型，功能是在已定义的list容器填充值    void assign( size_type count, const T& value );    template< class InputIt >    void assign( InputIt first, InputIt last );  //***********************************    例子：    #include <list>    #include <iostream>     int main()    &#123;        std::list<char> characters;        //若定义characters时并初始化为字符b，下面的填充操作一样有效        //std::list<char>characters(5,'b')         characters.assign(5, 'a');         for (char c : characters) &#123;            std::cout << c << ' ';        &#125;         return 0;    &#125;    输出结果：a a a a a  ***********************************/    //这里是第一个版本void assign( size_type count, const T& value );  void assign(size_type __n, const _Tp& __val) &#123; _M_fill_assign(__n, __val); &#125;  //这里为什么要把_M_fill_assign这个函数放在public呢？？保护起来不是更好吗？？  void _M_fill_assign(size_type __n, const _Tp& __val);#ifdef __STL_MEMBER_TEMPLATES  //以下是针对assign()函数的第二个版本  /*    template< class InputIt >    void assign( InputIt first, InputIt last );    这里有偏特化的现象，判断输入数据类型是否为整数型别  */  template <class _InputIterator>  void assign(_InputIterator __first, _InputIterator __last) &#123;    typedef typename _Is_integer<_InputIterator>::_Integral _Integral;    _M_assign_dispatch(__first, __last, _Integral());  &#125;  //若输入数据类型为整数型别，则派送到此函数  template <class _Integer>  void _M_assign_dispatch(_Integer __n, _Integer __val, __true_type)    &#123; _M_fill_assign((size_type) __n, (_Tp) __val); &#125;  //若输入数据类型不是整数型别，则派送到此函数  template <class _InputIterator>  void _M_assign_dispatch(_InputIterator __first, _InputIterator __last,                          __false_type);#endif /* __STL_MEMBER_TEMPLATES */  //assign()函数定义结束  //*********************************protected:    //把区间[first,last）的节点数据插入到指定节点position之前,position不能在区间内部    //这个函数是list类的protected属性，不是公共接口，只为list类成员服务    //为下面拼接函数void splice()服务  void transfer(iterator __position, iterator __first, iterator __last) &#123;    if (__position != __last) &#123;      // Remove [first, last) from its old position.      __last._M_node->_M_prev->_M_next     = __position._M_node;      __first._M_node->_M_prev->_M_next    = __last._M_node;      __position._M_node->_M_prev->_M_next = __first._M_node;       // Splice [first, last) into its new position.      _List_node_base* __tmp      = __position._M_node->_M_prev;      __position._M_node->_M_prev = __last._M_node->_M_prev;      __last._M_node->_M_prev     = __first._M_node->_M_prev;       __first._M_node->_M_prev    = __tmp;    &#125;  &#125;public:    //******************************    //***********拼接操作对外接口*************    //把链表拼接到当前链表指定位置position之前    /*void splice(const_iterator pos, list& other);        //把it在链表other所指的位置拼接到当前链表pos之前，it和pos可指向同一链表    void splice(const_iterator pos, list& other, const_iterator it);    //把链表other的节点范围[first,last)拼接在当前链表所指定的位置pos之前    //[first,last)和pos可指向同一链表    void splice(const_iterator pos, list& other,            const_iterator first, const_iterator last);    *******************************/    //******************************    //将链表x拼接到当前链表的指定位置position之前    //这里x和*this必须不同，即是两个不同的链表  void splice(iterator __position, list& __x) &#123;    if (!__x.empty())       this->transfer(__position, __x.begin(), __x.end());  &#125;  //将i所指向的节点拼接到position所指位置之前  //注意：i和position可以指向同一个链表  void splice(iterator __position, list&, iterator __i) &#123;    iterator __j = __i;    ++__j;    //若i和position指向同一个链表，且指向同一位置    //或者i和position指向同一个链表，且就在position的直接前驱位置    //针对以上这两种情况，不做任何操作    if (__position == __i || __position == __j) return;    //否则，进行拼接操作    this->transfer(__position, __i, __j);  &#125;  //将范围[first,last)内所有节点拼接到position所指位置之前  //注意：[first,last)和position可指向同一个链表，  //但是position不能在[first,last)范围之内  void splice(iterator __position, list&, iterator __first, iterator __last) &#123;    if (__first != __last)       this->transfer(__position, __first, __last);  &#125;  //以下是成员函数声明，定义在list类外实现  //******************************  //删除链表中值等于value的所有节点  void remove(const _Tp& __value);  //删除连续重复的元素节点，使之唯一  //注意：是连续的重复元素  void unique();  //合并两个已排序的链表  void merge(list& __x);  //反转链表容器的内容  void reverse();  //按升序排序链表内容  void sort();#ifdef __STL_MEMBER_TEMPLATES  template <class _Predicate> void remove_if(_Predicate);  template <class _BinaryPredicate> void unique(_BinaryPredicate);  template <class _StrictWeakOrdering> void merge(list&, _StrictWeakOrdering);  template <class _StrictWeakOrdering> void sort(_StrictWeakOrdering);#endif /* __STL_MEMBER_TEMPLATES */&#125;;</code></pre><h2 id="list容器的操作符重载"><a href="#list容器的操作符重载" class="headerlink" title="list容器的操作符重载"></a>list容器的操作符重载</h2><p>poerator==, &lt;, !=等等</p><pre class=" language-c++"><code class="language-c++">//运算符操作符重载template <class _Tp, class _Alloc>inline bool operator==(const list<_Tp,_Alloc>& __x, const list<_Tp,_Alloc>& __y)&#123;  typedef typename list<_Tp,_Alloc>::const_iterator const_iterator;  const_iterator __end1 = __x.end();  const_iterator __end2 = __y.end();  const_iterator __i1 = __x.begin();  const_iterator __i2 = __y.begin();  while (__i1 != __end1 && __i2 != __end2 && *__i1 == *__i2) &#123;    ++__i1;    ++__i2;  &#125;  return __i1 == __end1 && __i2 == __end2;&#125;template <class _Tp, class _Alloc>inline bool operator<(const list<_Tp,_Alloc>& __x,                      const list<_Tp,_Alloc>& __y)&#123;  return lexicographical_compare(__x.begin(), __x.end(),                                 __y.begin(), __y.end());&#125;#ifdef __STL_FUNCTION_TMPL_PARTIAL_ORDERtemplate <class _Tp, class _Alloc>inline bool operator!=(const list<_Tp,_Alloc>& __x,                       const list<_Tp,_Alloc>& __y) &#123;  return !(__x == __y);&#125;template <class _Tp, class _Alloc>inline bool operator>(const list<_Tp,_Alloc>& __x,                      const list<_Tp,_Alloc>& __y) &#123;  return __y < __x;&#125;template <class _Tp, class _Alloc>inline bool operator<=(const list<_Tp,_Alloc>& __x,                       const list<_Tp,_Alloc>& __y) &#123;  return !(__y < __x);&#125;template <class _Tp, class _Alloc>inline bool operator>=(const list<_Tp,_Alloc>& __x,                       const list<_Tp,_Alloc>& __y) &#123;  return !(__x < __y);&#125;//交换两个链表内容template <class _Tp, class _Alloc>inline void swap(list<_Tp, _Alloc>& __x, list<_Tp, _Alloc>& __y)&#123;  __x.swap(__y);&#125;#endif /* __STL_FUNCTION_TMPL_PARTIAL_ORDER */</code></pre><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul><li>在list中任何位置进行插入和删除的速度都很快</li><li>链表是一种非连续的结构，当用户要访问下一个元素的时候，迭代器要能够通过当前指向的位置的指针的 next 域，定位到正确访问的下一个位置，其迭代器是智能指针</li></ul><p>参考资料：<br>《STL源码剖析》侯捷<br>各位大佬博客</p>]]></content>
      
      
      <categories>
          
          <category> STL </category>
          
      </categories>
      
      
        <tags>
            
            <tag> STL, C++ - Markdown </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>STL容器源码剖析 (vector)</title>
      <link href="2021/05/17/vector/"/>
      <url>2021/05/17/vector/</url>
      
        <content type="html"><![CDATA[<h1 id="STL容器源码剖析-vector"><a href="#STL容器源码剖析-vector" class="headerlink" title="STL容器源码剖析 (vector)"></a>STL容器源码剖析 (vector)</h1><p>STL在日程编程应用非常的广泛,之前看到一篇大牛文章说，用C++开发，尽量用容器类+迭代器来代替数组+指针，因为数组+指针容易越界，或者内存泄露，相反，容器类和+迭代器都有大神将底层封装好，使用安全简单。</p><p>vector是有序容器里使用最广泛的容器,基本能够支持任何类型的对象，同时也是一个可以动态增长的数组。vector容器有已使用空间和可用空间，已使用空间是指vector容器的大小，可用空间是指vector容器可容纳的最大数据空间capacity。vector的实现依赖于内存的配置和内存的初始化，以及迭代器。其中内存的配置是最重要的，因为每当配置内存空间时，可能会发生数据移动，回收旧的内存空间，如果不断地重复这些操作会降低操作效率，所有vector容器在分配内存时，并不是用户数据占多少就分配多少，它会分配一些内存空间留着备用，即是用户可用空间。关于vector类定义可参考<a href="https://zh.cppreference.com/w/cpp/container/vector">vector</a>,以下源代码在SGI STL的文件&lt;stl_vector.h&gt;中。</p><h2 id="vector容器的数据结构"><a href="#vector容器的数据结构" class="headerlink" title="vector容器的数据结构"></a>vector容器的数据结构</h2><p>vector使用两个迭代器来管理连续内存空间，分别是指向目前使用空间的头start和指向目前使用空间的尾finish。还有以迭代器end_of_storage作为可用空间的尾。具体如下，</p><pre class=" language-c++"><code class="language-c++">//Alloc是SGI STL的空间配置器，默认是第二级配置器template <class _Tp, class _Alloc = __STL_DEFAULT_ALLOCATOR(_Tp) >class vector : protected _Vector_base<_Tp, _Alloc> &#123;       ...protected:  _Tp* _M_start;//表示目前使用空间的头  _Tp* _M_finish;//表示目前使用空间的尾  _Tp* _M_end_of_storage;//表示目前可用空间的尾      ...&#125;;</code></pre><h2 id="vector迭代器"><a href="#vector迭代器" class="headerlink" title="vector迭代器"></a>vector迭代器</h2><p>vector容器维护的空间的线性连续的，所以普通指针也可以作为迭代器。如：operator*，operator-&gt;，operator++，operator–，operator+，operator-，operator+=，operator-=等操作。</p><pre class=" language-c++"><code class="language-c++">//Alloc是SGI STL的空间配置器，默认是第二级配置器template <class _Tp, class _Alloc = __STL_DEFAULT_ALLOCATOR(_Tp) >class vector : protected _Vector_base<_Tp, _Alloc> &#123;  public://vector的内嵌型别定义,是iterator_traits<I>服务的类型  typedef _Tp value_type;  typedef value_type* pointer;  typedef const value_type* const_pointer;  typedef value_type* iterator;//vector容器的迭代器是普通指针  typedef const value_type* const_iterator;  ...  public://以下定义vector迭代器  iterator begin() &#123; return _M_start; &#125;//指向已使用空间头的迭代器  const_iterator begin() const &#123; return _M_start; &#125;  iterator end() &#123; return _M_finish; &#125;//指向已使用空间尾的迭代器  const_iterator end() const &#123; return _M_finish; &#125;  reverse_iterator rbegin()    &#123; return reverse_iterator(end()); &#125;  const_reverse_iterator rbegin() const    &#123; return const_reverse_iterator(end()); &#125;  reverse_iterator rend()    &#123; return reverse_iterator(begin()); &#125;  const_reverse_iterator rend() const    &#123; return const_reverse_iterator(begin()); &#125;    ...&#125;;</code></pre><h2 id="vector的构造函数和析构函数"><a href="#vector的构造函数和析构函数" class="headerlink" title="vector的构造函数和析构函数"></a>vector的构造函数和析构函数</h2><p>我们平时定义vector对象是会用到，如 vector<int> vec,vector<char> str(n)等等</p><pre class=" language-c++"><code class="language-c++"> /*以下是vector容器的构造函数***********************/***************************************    //默认构造函数****************************    explicit vector( const Allocator& alloc = Allocator() );          ****    //具有初始值和容器大小的构造函数********************    explicit vector( size_type count,                                  **              const T& value = T(),                               **              const Allocator& alloc = Allocator());              **      vector( size_type count,                                    **              const T& value,                                     **              const Allocator& alloc = Allocator());              ****    //只有容器大小的构造函数************************    explicit vector( size_type count );                               ****    //用两个迭代器区间表示容器大小的构造函数****************    template< class InputIt >                                         **    vector( InputIt first, InputIt last,                              **     const Allocator& alloc = Allocator() );                      *  ***    //拷贝构造函数****************************    vector( const vector& other );                                    **    vector( const vector& other, const Allocator& alloc );            * ***    //移动构造函数****************************    vector( vector&& other );                                         **    vector( vector&& other, const Allocator& alloc );                 ****    //用初始列表的值构造容器，列表内的元素值可以不同************    vector( std::initializer_list<T> init,                            **     const Allocator& alloc = Allocator() );                      **************************************/   explicit vector(const allocator_type& __a = allocator_type())    : _Base(__a) &#123;&#125;//默认构造函数  vector(size_type __n, const _Tp& __value,         const allocator_type& __a = allocator_type())     : _Base(__n, __a)//构造函数，里面包含n个初始值为value的元素    //全局函数，填充值函数，即从地址M_start开始连续填充n个初始值为value的元素    &#123; _M_finish = uninitialized_fill_n(_M_start, __n, __value); &#125;  explicit vector(size_type __n)//该构造函数不接受初始值，只接受容易包含元素的个数n    : _Base(__n, allocator_type())    &#123; _M_finish = uninitialized_fill_n(_M_start, __n, _Tp()); &#125;  vector(const vector<_Tp, _Alloc>& __x)     : _Base(__x.size(), __x.get_allocator())//拷贝构造函数    &#123; _M_finish = uninitialized_copy(__x.begin(), __x.end(), _M_start); &#125;#ifdef __STL_MEMBER_TEMPLATES  // Check whether it's an integral type.  If so, it's not an iterator.  /*这个是某个区间的构造函数，首先判断输入是否为整数_Integral()  *采用__type_traits技术  */  template <class _InputIterator>  vector(_InputIterator __first, _InputIterator __last,         const allocator_type& __a = allocator_type()) : _Base(__a) &#123;    typedef typename _Is_integer<_InputIterator>::_Integral _Integral;    _M_initialize_aux(__first, __last, _Integral());  &#125;  template <class _Integer>  //若输入为整数，则调用该函数  void _M_initialize_aux(_Integer __n, _Integer __value, __true_type) &#123;    _M_start = _M_allocate(__n);    _M_end_of_storage = _M_start + __n;     _M_finish = uninitialized_fill_n(_M_start, __n, __value);  &#125;  template <class _InputIterator>  //若输入不是整数，则采用Traits技术继续判断迭代器的类型  void _M_initialize_aux(_InputIterator __first, _InputIterator __last,                         __false_type) &#123;    _M_range_initialize(__first, __last, __ITERATOR_CATEGORY(__first));  &#125;#else  vector(const _Tp* __first, const _Tp* __last,         const allocator_type& __a = allocator_type())    : _Base(__last - __first, __a)     &#123; _M_finish = uninitialized_copy(__first, __last, _M_start); &#125;#endif /* __STL_MEMBER_TEMPLATES */  ~vector() &#123; destroy(_M_start, _M_finish); &#125;//析构函数</code></pre><h2 id="vector的成员函数"><a href="#vector的成员函数" class="headerlink" title="vector的成员函数"></a>vector的成员函数</h2><p>vector的方法，如push_back(x),pop_back(),insert()等等</p><pre class=" language-c++"><code class="language-c++">    /*以下是容器的一些成员函数*/  size_type size() const//vector容器大小(已使用空间大小)，即容器内存储元素的个数    &#123; return size_type(end() - begin()); &#125;  size_type max_size() const//返回可容纳最大元素数    &#123; return size_type(-1) / sizeof(_Tp); &#125;  size_type capacity() const//vector容器可用空间的大小    &#123; return size_type(_M_end_of_storage - begin()); &#125;  bool empty() const//判断容器是否为空    &#123; return begin() == end(); &#125;  reference operator[](size_type __n) &#123; return *(begin() + __n); &#125;//返回指定位置的元素  const_reference operator[](size_type __n) const &#123; return *(begin() + __n); &#125;#ifdef __STL_THROW_RANGE_ERRORS  //若用户要求的空间大于可用空间，抛出错去信息，即越界检查  void _M_range_check(size_type __n) const &#123;    if (__n >= this->size())      __stl_throw_range_error("vector");  &#125;  reference at(size_type __n)//访问指定元素，并且进行越界检查    &#123; _M_range_check(__n); return (*this)[__n]; &#125;//访问前，先进行越界检查  const_reference at(size_type __n) const    &#123; _M_range_check(__n); return (*this)[__n]; &#125;#endif /* __STL_THROW_RANGE_ERRORS */    void reserve(size_type __n) &#123;//改变可用空间内存大小    if (capacity() < __n) &#123;      const size_type __old_size = size();      //重新分配大小为n的内存空间，并把原来数据复制到新分配空间      iterator __tmp = _M_allocate_and_copy(__n, _M_start, _M_finish);      destroy(_M_start, _M_finish);//释放容器元素对象      _M_deallocate(_M_start, _M_end_of_storage - _M_start);//回收原来的内存空间      //调整迭代器所指的地址,因为原来迭代器所指的地址已经失效      _M_start = __tmp;      _M_finish = __tmp + __old_size;      _M_end_of_storage = _M_start + __n;    &#125;  &#125;  reference front() &#123; return *begin(); &#125;//返回第一个元素  const_reference front() const &#123; return *begin(); &#125;  reference back() &#123; return *(end() - 1); &#125;//返回容器最后一个元素  const_reference back() const &#123; return *(end() - 1); &#125;  void push_back(const _Tp& __x) &#123;//在最尾端插入元素    if (_M_finish != _M_end_of_storage) &#123;//若有可用的内存空间      construct(_M_finish, __x);//构造对象      ++_M_finish;    &#125;    else//若没有可用的内存空间,调用以下函数，把x插入到指定位置      _M_insert_aux(end(), __x);  &#125;  void push_back() &#123;    if (_M_finish != _M_end_of_storage) &#123;      construct(_M_finish);      ++_M_finish;    &#125;    else      _M_insert_aux(end());  &#125;  void swap(vector<_Tp, _Alloc>& __x) &#123;      /*交换容器的内容      *这里使用的方法是交换迭代器所指的地址      */    __STD::swap(_M_start, __x._M_start);    __STD::swap(_M_finish, __x._M_finish);    __STD::swap(_M_end_of_storage, __x._M_end_of_storage);  &#125;  iterator insert(iterator __position, const _Tp& __x) &#123;//把x值插入到指定的位置    size_type __n = __position - begin();    if (_M_finish != _M_end_of_storage && __position == end()) &#123;      construct(_M_finish, __x);      ++_M_finish;    &#125;    else      _M_insert_aux(__position, __x);    return begin() + __n;  &#125;  iterator insert(iterator __position) &#123;    size_type __n = __position - begin();    if (_M_finish != _M_end_of_storage && __position == end()) &#123;      construct(_M_finish);      ++_M_finish;    &#125;    else      _M_insert_aux(__position);    return begin() + __n;  &#125;  void insert (iterator __pos, size_type __n, const _Tp& __x)    &#123; //在pos位置连续插入n个初始值为x的元素        _M_fill_insert(__pos, __n, __x); &#125;  void _M_fill_insert (iterator __pos, size_type __n, const _Tp& __x);  void pop_back() &#123;//取出最尾端元素    --_M_finish;    destroy(_M_finish);//析构对象  &#125;  iterator erase(iterator __position) &#123;//擦除指定位置元素    if (__position + 1 != end())      copy(__position + 1, _M_finish, __position);//后续元素前移一位    --_M_finish;    destroy(_M_finish);//析构对象    return __position;  &#125;  iterator erase(iterator __first, iterator __last) &#123;//擦除两个迭代器区间的元素    iterator __i = copy(__last, _M_finish, __first);//把不擦除的元素前移    destroy(__i, _M_finish);//析构对象    _M_finish = _M_finish - (__last - __first);//调整finish的所指的位置    return __first;  &#125;  void resize(size_type __new_size, const _Tp& __x) &#123;//改变容器中可存储的元素个数，并不会分配新的空间    if (__new_size < size()) //若调整后的内存空间比原来的小      erase(begin() + __new_size, end());//擦除多余的元素    else      insert(end(), __new_size - size(), __x);//比原来多余的空间都赋予初值x  &#125;  void resize(size_type __new_size) &#123; resize(__new_size, _Tp()); &#125;  void clear() &#123; erase(begin(), end()); &#125;//清空容器     // assign(), a generalized assignment member function.  Two  // versions: one that takes a count, and one that takes a range.  // The range version is a member template, so we dispatch on whether  // or not the type is an integer.  /*该函数有两种类型：    void assign( size_type count, const T& value );    template< class InputIt >    void assign( InputIt first, InputIt last );    */  //把容器内容替换为n个初始值为value  void assign(size_type __n, const _Tp& __val) &#123; _M_fill_assign(__n, __val); &#125;  void _M_fill_assign(size_type __n, const _Tp& __val);#ifdef __STL_MEMBER_TEMPLATES    template <class _InputIterator>  void assign(_InputIterator __first, _InputIterator __last) &#123;    typedef typename _Is_integer<_InputIterator>::_Integral _Integral;    _M_assign_dispatch(__first, __last, _Integral());  &#125;  template <class _Integer>  void _M_assign_dispatch(_Integer __n, _Integer __val, __true_type)    &#123; _M_fill_assign((size_type) __n, (_Tp) __val); &#125;  template <class _InputIter>  void _M_assign_dispatch(_InputIter __first, _InputIter __last, __false_type)    &#123; _M_assign_aux(__first, __last, __ITERATOR_CATEGORY(__first)); &#125;  template <class _InputIterator>  void _M_assign_aux(_InputIterator __first, _InputIterator __last,                     input_iterator_tag);  template <class _ForwardIterator>  void _M_assign_aux(_ForwardIterator __first, _ForwardIterator __last,                     forward_iterator_tag); #endif /* __STL_MEMBER_TEMPLATES */</code></pre><p>其中特别的是：iterator erase(iterator __first, iterator __last)函数只是擦除输入迭代器之间的元素，但是没有回收内存空间，只是把内存空间作为备用空间。<br>void insert (iterator __pos, size_type __n, const _Tp&amp; __x)函数插入元素是在指定位置position上连续插入n个初始值为x的元素，根据插入元素个数和可用空间大小的比较，分别进行不同的初始化，详细见源码分析。</p><h2 id="vector的操作符重载"><a href="#vector的操作符重载" class="headerlink" title="vector的操作符重载"></a>vector的操作符重载</h2><p>如 =, !=, ==, &lt;, &gt;等等</p><pre class=" language-c++"><code class="language-c++">template <class _Tp, class _Alloc>inline bool //操作符重载，判断两个容器是否相等，即容器大小和容器内容是否都相等operator==(const vector<_Tp, _Alloc>& __x, const vector<_Tp, _Alloc>& __y)&#123;  return __x.size() == __y.size() &&         equal(__x.begin(), __x.end(), __y.begin());  /*STL中equal函数的实现如下：    * template<class InputIt1, class InputIt2>    * bool equal(InputIt1 first1, InputIt1 last1, InputIt2 first2)    * &#123;    *    for (; first1 != last1; ++first1, ++first2)     *    &#123;    *        if (!(*first1 == *first2))     *        &#123;    *            return false;    *        &#125;    *    &#125;    *    return true;    * &#125;    */&#125;template <class _Tp, class _Alloc>inline bool operator<(const vector<_Tp, _Alloc>& __x, const vector<_Tp, _Alloc>& __y)&#123;  /*函数原型：    template<class InputIt1, class InputIt2>    bool lexicographical_compare(InputIt1 first1, InputIt1 last1,                             InputIt2 first2, InputIt2 last2)    &#123;        for ( ; (first1 != last1) && (first2 != last2); first1++, first2++ ) &#123;            if (*first1 < *first2) return true;            if (*first2 < *first1) return false;            &#125;        return (first1 == last1) && (first2 != last2);    &#125;      */    return lexicographical_compare(__x.begin(), __x.end(),                                  __y.begin(), __y.end());&#125;#ifdef __STL_FUNCTION_TMPL_PARTIAL_ORDERtemplate <class _Tp, class _Alloc>inline void swap(vector<_Tp, _Alloc>& __x, vector<_Tp, _Alloc>& __y)&#123;  __x.swap(__y);&#125;template <class _Tp, class _Alloc>inline booloperator!=(const vector<_Tp, _Alloc>& __x, const vector<_Tp, _Alloc>& __y) &#123;  return !(__x == __y);&#125;template <class _Tp, class _Alloc>inline booloperator>(const vector<_Tp, _Alloc>& __x, const vector<_Tp, _Alloc>& __y) &#123;  return __y < __x;&#125;template <class _Tp, class _Alloc>inline booloperator<=(const vector<_Tp, _Alloc>& __x, const vector<_Tp, _Alloc>& __y) &#123;  return !(__y < __x);&#125;template <class _Tp, class _Alloc>inline booloperator>=(const vector<_Tp, _Alloc>& __x, const vector<_Tp, _Alloc>& __y) &#123;  return !(__x < __y);&#125;#endif /* __STL_FUNCTION_TMPL_PARTIAL_ORDER */template <class _Tp, class _Alloc>vector<_Tp,_Alloc>& vector<_Tp,_Alloc>::operator=(const vector<_Tp, _Alloc>& __x)&#123;  if (&__x != this) &#123;    const size_type __xlen = __x.size();    if (__xlen > capacity()) &#123;      iterator __tmp = _M_allocate_and_copy(__xlen, __x.begin(), __x.end());      destroy(_M_start, _M_finish);      _M_deallocate(_M_start, _M_end_of_storage - _M_start);      _M_start = __tmp;      _M_end_of_storage = _M_start + __xlen;    &#125;    else if (size() >= __xlen) &#123;      iterator __i = copy(__x.begin(), __x.end(), begin());      destroy(__i, _M_finish);    &#125;    else &#123;      copy(__x.begin(), __x.begin() + size(), _M_start);      uninitialized_copy(__x.begin() + size(), __x.end(), _M_finish);    &#125;    _M_finish = _M_start + __xlen;  &#125;  return *this;&#125;</code></pre><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul><li>vector的各种操作其实离不开四个操作，堆内存的申请和释放对象的创建和摧毁</li><li>对vector执行插入删除操作，此时的迭代器是不安全的，需要重新获取，如果引起了新的空间配置 那么指向vector的所有迭代器都会失效。</li><li>当调用erase函数时，size()函数会变化，capacity不会发生变化。</li></ul><p>参考资料：<br>《STL源码剖析》侯捷<br>各位大佬博客</p>]]></content>
      
      
      <categories>
          
          <category> STL </category>
          
      </categories>
      
      
        <tags>
            
            <tag> STL, C++ - Markdown </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
